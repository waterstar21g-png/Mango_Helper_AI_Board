버전 2.8.3 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 — 작업 URL 을 필수 입력으로.
1) 원인 — 보드 「작업 URL」 칸에 DEFAULT_LIST_URL 을 미리 채워뒀는데 그 기본값이
   admin_group.php?pmode=filter_delete&uids=& 라 목록이 렌더링되지 않았다.
   그대로 실행하면 행 0건으로 끝난다.
2) 이제 기본값을 채우지 않고 [필수] 로 표기한다. 비워두고 실행하면
   '작업 URL 을 입력하세요' 로 즉시 중단한다 — 되돌릴 수 없는 작업이라
   엉뚱한 화면에서 돌지 않게 막는다.
3) 목록 화면 주소창 URL 을 그대로 붙여넣으면 된다.

버전 2.8.2 (Python 보드)
업데이트: 상품수집사이트 선택 실패 수정 · 진단 로그 보강.
1) 원인 — 입력한 사이트명을 옵션과 **정확 일치**로만 찾았다. 그래서
   `MUSINSA.COM` 입력이 화면 옵션 `MUSINSA.com` 을 못 찾고 실패했다.
   이제 정확 → 대소문자·공백 무시 → 부분 포함 순으로 맞춘다
   (P3_필터단위_수집조건수정 의 match_option 과 같은 방식).
   실패 시 화면의 옵션 목록을 로그에 그대로 남긴다.
2) 진단 로그 — URL 을 80자에서 자르지 않고 통째로 남긴다. 목록 표가 아예 없으면
   「작업 URL 이 검색필터 목록 화면이 맞는지」 안내를 덧붙인다.
   상품수집사이트 옵션 목록도 함께 출력한다.
3) P5_101 · P3_설정수정_카테고리매핑초기화 둘 다 같은 select_site 를 쓰므로
   함께 고쳐진다.

버전 2.8.1 (Python 보드)
업데이트: 바탕화면 아이콘을 작업표시줄에 고정 가능하도록.
1) 원인 1 — 바로가기 타깃이 run.bat 이었다. Windows 는 .bat 을 직접 가리키는
   바로가기에 「작업 표시줄에 고정」 을 아예 제공하지 않는다. 이제 타깃을
   cmd.exe 로 두고 run.bat 을 인자(/c "…\run.bat")로 넘겨 고정이 가능해졌다.
   동작·작업폴더·아이콘은 그대로다.
2) 원인 2 — 고정 verb 를 한글/영문 문자열 하드코딩으로 찾았다. 이제 shell32.dll
   문자열 리소스 5386 에서 현지화된 이름을 읽어 1순위로 대조한다 (기존 패턴은 폴백).
3) 자동 고정이 막히면(Win10 1903+ · Win11) 아이콘을 선택한 탐색기 창을 띄워
   우클릭 → [작업 표시줄에 고정] 을 바로 누를 수 있게 했다. 안내문도 정확히 바꿨다.
4) 망고보드_작업표시줄고정.bat 신설 — 아이콘 재생성 없이 고정만
   (board/desktop_icon.py --pin-only).

버전 2.8.0 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 신설.
1) P3_필터단위_수집조건수정 을 복제하고, 목록·팝업 조작은 P5_101 의 검증된 로직
   (map_categories.py) 을 재사용 — 같은 화면(admin_group.php 목록 ·
   market_mapping_new 팝업 · admin_category_set.php)을 다루기 때문.
2) 입력: 사이트명 · 작업 URL(작업 시작 시 [선택조건으로 검색하기] 자동 클릭) ·
   작업행 범위 [부터]-[까지].
3) 행마다: [설정수정](market_mapping_new) → 팝업 → [검색필터 설정삭제]
   (config_remove('','Y')) → 팝업 닫기 → 다음 행.
4) --list-rows 로 초기화 없이 행 번호·ftid·필터명 확인 가능 (P5_101 과 동일).
5) 보드 P3_설정수정_카테고리매핑초기화 탭 — [행 목록 확인] · [초기화 시작]
   (되돌릴 수 없음 확인 대화상자) · [작업중단].

버전 2.7.3 (Python 보드)
업데이트: 망고보드_독립저장소올리기.bat 구문 오류 수정.
1) 원인 — scripts/*.ps1 이 BOM 없는 UTF-8 이라, Windows PowerShell 5.1 이
   한글 Windows 기본 코드페이지(CP949)로 읽었다. 한글·기호(→·※)가 2바이트로
   잘못 소비되면서 뒤따르는 큰따옴표를 삼켜 문자열이 깨지고,
   'Unexpected token }' 구문 오류가 났다 (publish-standalone.ps1:85).
2) scripts/*.ps1 4개 전부에 UTF-8 BOM 을 넣어 PowerShell 이 UTF-8 로 읽게 했다.
   install-all.ps1 도 같은 결함이 있었다 (따옴표 2개 소실).
3) board/tests/test_ps1_encoding.py 신설 — BOM 누락과 CP949 오해석 시
   따옴표 개수가 달라지는 경우를 잡는다.

버전 2.7.2 (Python 보드)
업데이트: P5_101 화면 검색결과 선택에도 성별 규칙 적용.
1) 원인 — 성별 배제(2.6.0)가 엑셀 카테고리를 고르는 단계에만 걸려 있었다.
   검색어는 카테고리의 리프뿐이라(예 '로퍼') 망고가 남성·여성을 함께 돌려주는데,
   pick_option 이 리프만 같으면 목록에 먼저 나온 항목을 골라 여성 필터가
   '남성신발 > 로퍼' 로 매핑됐다.
2) pick_option 에 필터명을 넘겨 반대 성별 항목을 걷어내고 같은 성별을 우선한다.
   반대 성별만 남으면 매핑하지 않고 사유를 로그에 남긴다.
3) 선택 직후 violates_gender 재검사 추가 — 위반이면 저장하지 않는다.
4) 성별 판정 오류 수정 — 'women' 안에 'men' 이 들어 있어 'Womens Shoes' 를
   남성으로 읽었다. 남성 판정 전에 여성 영문 표기를 제외하고,
   여성 표기에 woman·ladies 를 추가했다.

버전 2.7.1 (Python 보드)
업데이트: '몇 번째 행인지' 확인 기능 추가.
1) map_categories.py --list-rows — 매핑 없이 목록을 열어 1행부터 번호·ftid·
   필터명을 로그로 출력. 지정한 --row-from/--row-to 구간에 ★ 표시.
2) 보드 P5_101 탭에 [행 목록 확인] 버튼 추가 — 매핑 시작 전에 행 번호를 검증.
3) 번호는 [설정수정](market_mapping_new) 링크가 화면에 나오는 순서(위→아래)와
   같다.

버전 2.7.0 (Python 보드)
업데이트: 품목 계열 배타 · 하위 우선 검색.
1) 계열 사전(의류·신발·모자·선글라스·시계·가방·액세서리) 신설 — 다른 계열 카테고리는
   후보에서 제외한다 (의류↔신발, 선글라스↔시계 등). 계열 불명·잡화 계열은 남긴다.
2) 같은 계열이 없으면 잡화 계열에서 선택 (규칙3 폴백).
3) 전체 재검색 순서를 하위(1차) → 중위(2차) 로 교체, 하위 검색에 동의어 확장 적용.

버전 2.6.0 (Python 보드)
업데이트: 반대 성별 카테고리 절대 배제.
1) 여성 필터 → '남성' 용어가 든 카테고리(남성패션·남성신발 등) 전부 후보 제거,
   남성 필터 → '여성' 용어 카테고리 전부 제거. 최근접 지정 단계에도 동일 적용.
2) 최종 선택 직전 재검사 — 위반 시 배제 후 재선정 (로그 표기).
3) 반대 성별만 있어 고를 것이 없으면 매핑하지 않고 사유를 남긴다
   ('성별(여성) 조건에 맞는 카테고리 없음').

버전 2.5.2 (Python 보드)
업데이트: 보드 P5_101 입력 배치 정리.
1) 「작업 행 범위 [부터]-[까지]」 를 상품수집사이트 줄에서 떼어 「목록 URL」 아래로 이동.
2) 그 옆에 기준 안내 표기 — 목록 URL 검색결과의 행 번호(1부터, 양끝 포함).

버전 2.5.1 (Python 보드)
업데이트: '작업 대상 행이 없습니다' 수정.
1) 원인 — 행 스캔을 table#search_category 로 한정해, 설정수정 링크가 15개 있는
   화면에서도 0행으로 읽혔다(진단 로그로 확인).
2) 이제 표 구조에 기대지 않고 a[onclick*=market_mapping_new] 에서 closest('tr') 로
   행을 찾는다. 필터이름은 행의 텍스트 input → attr-uid 매칭 input → 행 첫 줄 순.
3) 모든 프레임 결과를 합치고 ftid 중복은 제거 (프레임에 중복 노출되는 화면 대응).

버전 2.5.0 (Python 보드)
업데이트: 카테고리 선택 규칙 4가지를 강제 적용.
1) 엑셀 범위 내 선택 (기존) — 확정 후 검사·교정.
2) 성별 고정 — 남성/여성이 구분된 필터는 같은 성별 경로에서만 선택,
   같은 성별이 없으면 무성별 경로 (다른 성별 배제).
3) 품목명 일치 — 의류·잡화·모자·선글라스·액세서리·신발이 필터명에 있으면
   그 명칭이 든 카테고리에서만 고르고, 상위→중위→하위 순으로 우선.
4) 의류 우선순위 — 패션의류잡화·패션의류·남성/여성(패션)의류를 먼저 선택
   (예: 남성-팬츠는 스포츠/레저보다 패션의류).
적용된 규칙은 로그에 [성별=남성 · 품목=의류] 로 표기.

버전 2.4.1 (Python 보드)
업데이트: 최적 카테고리는 반드시 엑셀 범위 내에서 선택.
1) matching.is_from / ensure_from 추가 — 결과가 엑셀 목록 밖이면 목록 내
   최근접 값으로 교정하고 로그에 남긴다.
2) 화면 검색 결과 선택도 엑셀 카테고리 기준으로 맞춘다 (필터명 기준 아님).

버전 2.4.0 (Python 보드)
업데이트: 매핑 누락 방지 — 정밀 분석 보강 · 저장 후 재검증 3회.
1) 동의어 사전 추가 (바라클라바↔방한모, 버킷햇↔벙거지, 비니↔니트모자,
   스니커즈↔운동화, 선글라스↔아이웨어 …) — 최근접 판단에 반영.
   리프가 동의어와 일치하면 높은 가중치.
2) 저장 후 재검증 — openmarket_cm_category_<코드>/_name_<코드> hidden 값으로
   실제 매핑 여부를 확인하고, 미매핑 마켓만 다시 매핑 후 저장. 최대 3회 반복.
3) 3회 후에도 남으면 경고 로그 + 결과에 unmapped 기록.

버전 2.3.0 (Python 보드)
업데이트: 최적 카테고리를 못 찾아도 반드시 하나 지정 · 3회 재시도.
1) 규칙 탐색(1·2-1~2-4) 실패 시 '3) 최근접 지정' 단계 추가 — 소재·용도/활용·성별·
   글자 유사도를 종합해 가장 가까운 카테고리 하나를 반드시 고른다(성별 불일치 감점,
   품목 포괄 카테고리 가점).
2) 매핑 실패(검색결과 없음·목록 선택 실패) 시 다른 카테고리로 최대 3회 추가 시도
   (매번 직전 후보 제외), 이후 행 끝에서 검색필터 설정저장으로 마무리.
3) find_category(force=False) 로 기존처럼 미검출 반환도 가능.

버전 2.2.0 (Python 보드)
업데이트: P5_101 구분 라디오 · 상품고시정보 팝업 대응.
1) 보드 입력에 라디오 추가 — 11번가(해외/국내/둘다) · 롯데ON(해외직구/일반/둘다),
   기본 '둘다'. 둘다면 한 행에서 두 구분을 각각 매핑한다.
   구분 라디오는 openmarket_seller_type2_<코드> 를 라벨로 찾아 강제 클릭.
2) 매핑 후 상품고시정보 팝업(#mapping_notify_<코드>)이 뜨면 닫고,
   직전 카테고리를 제외한 다음 최적 카테고리로 재매핑 후 설정저장 (최대 3회).
3) matching.find_category 에 exclude 지원 추가.

버전 2.1.1 (Python 보드)
업데이트: P5_101 체크 여부와 무관하게 행 범위로 작업.
1) '체크된 행이 없습니다' 로 멈추던 문제 — 이제 체크 상태를 보지 않고
   [부터]~[까지] 범위의 행을 그대로 처리한다 (로그에 체크 건수는 참고 표기).
2) 목록이 프레임 안에 있어도 찾도록 프레임 탐색 추가.
3) 행을 못 찾으면 진단 로그 출력 — 프레임별 url·tr 수·체크박스 수·설정수정 링크 수·
   onclick 예시.

버전 2.1.0 (Python 보드)
업데이트: P5_101 적용 범위 제한 (요건).
1) 수집사이트 musinsa.com 한정 — 다른 사이트는 실행 거부(보드 경고), 검증 후 개방.
2) 작업 대상은 [선택조건으로 검색하기] 결과 목록의 체크된 행만.
3) 작업 행 범위 입력 [부터]-[까지] 신설 (기본 1~5, 1부터·양끝 포함).
   범위가 뒤집히거나 잘못되면 자동 보정. 로그에 '체크 N건 중 a~b행 → m건 수행' 표기.

버전 2.0.1 (Python 보드)
업데이트: P5_101 최적 카테고리 탐색을 요건 명세대로 재구현 (matching.py 신설).
1) 필터명 해석 — 앞 2조각(브랜드-사이트) 무시, 상위·중위·하위로 분해.
   하위는 붙인말·토큰·반쪽으로 전개 (버킷/사파리 햇 → 버킷햇·버킷·햇·사파리,
   바라클라바 → 바라클라바·바라·클라바).
2) 찾는 순서 — 1)단계일치 → 2-1)상위→중위→하위 → 2-2)중위 전체 →
   2-3)하위 전체 → 2-4)품목별 포괄(의류/신발/잡화).
3) 상위 보조 후보(패션잡화·의류잡화·패션의류잡화)와 중위 동의어(소품↔잡화) 반영.
4) 어느 단계에서 찾았는지 실행로그·드라이런에 표기.

버전 2.0.0 (Python 보드)
업데이트: P5_101_카테고리매핑_필터세부설정 신설.
0) 마켓별 입력필드 #openmarket_category_search_text_<코드>, 결과 리스트박스는
   list_/list2_ 두 벌 중 보이는 select 우선 (11번가·롯데ON).
1) 초기 1회 — 상품수집사이트 선택(select[name=site_id]) → [선택조건으로 검색하기]
   (search_filter('search')) → 마켓별 카테고리 엑셀 로드(P5 출력 양식, 폴더 자동인식).
2) 1단계 루프 — 체크된 행마다 필터이름 읽기 → [설정수정](market_mapping_new(ftid))
   → 팝업(admin_category_set.php?ps_ftid=) → [AI 자동 매핑 시작하기]
   (search_recommend_category_all) → 마켓별 매핑 → [검색필터 설정저장](config_save)
   → 모달 닫기 → 다음 행.
3) 2단계 루프 — 옥션2.0·11번가·G마켓2.0·스마트스토어·쿠팡·롯데ON 순서로
   필터이름↔엑셀 카테고리 유사도 비교 → 검색필드 입력
   (#openmarket_category_search_text_<코드>) → [검색] → 결과 목록 선택.
4) 점수 0.34 미만이면 매칭하지 않고 건너뛴다 (오매핑 방지). --dry-run 으로
   브라우저 없이 매칭 결과만 확인 가능.
5) 보드 P5_101 탭 — 사이트·엑셀 폴더(인식 결과 표시)·목록 URL·시작/중단·로그.

버전 1.9.3 (Python 보드)
업데이트: 구분 라디오를 항상 클릭해 체크.
1) 이미 체크된 라디오는 check() 가 no-op 이라 onclick=change_category_list 가
   실행되지 않았다. 이제 force 클릭으로 항상 실행시키고, 기본 선택이었으면
   로그에 '(기본 선택 → 재클릭)' 을 남긴다.

버전 1.9.2 (Python 보드)
업데이트: 롯데ON 일반카테고리 누락 수정.
1) 원인 — 두 select(list_/list2_)이 같은 name 에 display 로만 전환되는데,
   '옵션이 많은 쪽'을 골라 숨은 select 의 직전 구분 목록을 읽을 수 있었다.
   이제 보이는 select(getComputedStyle·offsetParent) 을 우선 읽는다.
2) 구분 전환 후 목록 지문이 직전 구분과 달라질 때까지 대기(최대 15초),
   끝까지 같으면 경고 로그. 읽은 select id·건수를 로그에 표기.

버전 1.9.1 (Python 보드)
업데이트: P5 가 카테고리설정 화면을 전용 탭에서 열도록 수정.
1) 원인 — CDP 로 붙은 현재 탭(수집조건수정 팝업일 수 있음)에서 그대로 이동해
   엉뚱한 창이 바뀌었다. 이제 admin_category_set.php 탭이 있으면 재사용,
   없으면 새 탭을 만들어 앞으로 가져온다.
2) --ftid 옵션 추가 — 검색필터별 ps_ftid 로 접근 URL 생성 (기본 790).

버전 1.9.0 (Python 보드)
업데이트: P5 카테고리 구분(해외/국내) 각각 추출.
1) 11번가 — 해외카테고리 / 국내카테고리, 롯데ON — 해외직구 카테고리 / 일반카테고리.
   구분 라디오(openmarket_seller_type2_<코드>)를 label 내 span 텍스트로 찾아 선택하고
   change_category_list 반영을 기다린 뒤 [전체카테고리] 조회.
2) 엑셀에 '구분' 열 추가 — 마켓·구분·1~6단계·전체경로. 두 구분을 각각 담는다.
3) ALL 실행 시 마켓 × 구분 조합을 순서대로 추출하고 조합별 행수를 로그에 남긴다.

버전 1.8.2 (Python 보드)
업데이트: P5 구현 제외 마켓 고정.
1) LFMall · 머스트잇 · 쇼피 · 큐텐(일본) · 플레이오토(EMP) 는 추출 대상 제외.
   EXCLUDED_MARKETS 로 명시하고 ALL 실행에서도 걸러낸다. 직접 지정하면
   '구현 제외 마켓입니다' 로 종료.

버전 1.8.1 (Python 보드)
업데이트: P5 전체 마켓 지원.
1) 마켓 6종 — 옥션2.0(AUC20) · 11번가(11ST) · G마켓2.0(GMK20) ·
   스마트스토어(SMART) · 쿠팡(COUP) · 롯데ON(LTON).
2) --market ALL — 한 번에 6개 마켓을 순서대로 추출해 한 엑셀에 마켓 열로 정리.
3) 목록 select 가 list_/list2_ 두 벌인 마켓(11번가·롯데ON) 대응 — 둘 다 읽어
   항목이 많은 쪽 사용.
4) 보드 P5 마켓 리스트박스에 '전체 마켓 일괄' 추가.

버전 1.8.0 (Python 보드)
업데이트: P5_카테고리_엑셀추출 신설.
1) admin_category_set.php 에서 마켓(기본 옥션2.0/AUC20) [전체카테고리] 클릭 →
   select#openmarket_category_search_list_<마켓> 옵션 전체를 읽어
   카테고리분류표(1~6단계) 엑셀로 저장. 안내문구·중복 제외, 6단계 초과는 6단계에 합침.
2) 보드 P5 탭 — 마켓 리스트박스·접근URL·저장경로·[추출 시작]·[엑셀 열기]·로그.
3) --from-text 로 브라우저 없이 목록 텍스트만으로도 엑셀 생성 가능.

버전 1.7.6 (Python 보드)
업데이트: 수집사이트 드롭다운 미검출 실제 원인 수정.
1) 원인 — 다른 탭(select 0개)을 보고 있었다. pick_list_page 로 site_id 가 있는
   탭/프레임을 골라잡는다 (URL getGoodsCategory 폴백).
2) 시작 1회 단계이므로 수집사이트 대기는 5초(T_SITE), 행 반복 단계는 빠른 값 유지.
3) 진단 로그 강화 — 탭·프레임별 url · select 이름 · 버튼 라벨을 그대로 출력.

버전 1.7.5 (Python 보드)
업데이트: 수집사이트 드롭다운 미검출 수정.
1) 대기 0.3초 → 1초 (T_POPUP). 0.3초는 목록이 덜 뜬 상태에서 조회해 실패했다.
2) apply_site_filter/read_site_options 가 드롭다운을 '기다린 뒤' 찾는다
   (1초 단위 최대 3회, 뜨면 즉시 진행).
3) 프레임 전체 순회 + 「수집사이트」 옵션 텍스트 폴백 + 미검출 시 화면 select
   목록을 로그에 출력(원인 파악용).

버전 1.7.4 (Python 보드)
업데이트: 팝업 대기 예산 0.3초.
1) T_POPUP = 300ms — 팝업 열기(expect_popup)·직접 오픈(goto)·번역옵션 드롭다운 대기.
2) 드롭다운은 0.3초 단위로 최대 4회 확인(POPUP_TRIES) — 뜨는 즉시 진행하므로
   빠른 경우 손해가 없고, 서버가 느릴 때만 최대 1.2초까지 버틴다.

버전 1.7.3 (Python 보드)
업데이트: P3_필터단위_수집조건수정 — 순서 유지 · 속도 최대화 · 닫기 버튼.
1) 저장하기 직후 바로 옆 [닫기](onclick=window.close()) 를 눌러 팝업을 즉시 종료하고
   다음 행으로 진행. 못 찾으면 window.close() 직접 호출 → 그래도 남으면 강제 종료.
2) 단계별 대기 10배 축소 (클릭 1.5s · 필드 2s · 읽기 0.2s · 닫힘 0.8s ·
   행간 0.02s · 검색후 0.15s). 상수 T_*/GAP_* 한 곳에서 관리.
3) 목록 접속은 URL 직접 이동(빠른 경로) 우선, 실패 시에만 기존 단계 절차.
4) 순서(수집조건수정 → 팝업 → 번역조건 반영 → 저장 → 닫기 → 다음행)는 그대로.

버전 1.7.2 (Python 보드)
업데이트: 부트스트랩 복사 누락 방지.
1) AI보드 루트 망고보드_시작.bat 의 robocopy 에 /IS /IT 추가 — 타임스탬프가 같은
   옛 파일(app.py 등)이 남아 버전만 바뀌는 문제를 막는다.
2) board/tests/test_bootstrap_bat.py 신설 — 단계·강제덮어쓰기·데이터보호·대상폴더 검증.

버전 1.7.1 (Python 보드)
업데이트: 독립 저장소 올리기 원클릭.
1) 망고보드_독립저장소올리기.bat 신설 — scripts/publish-standalone.ps1 호출.
2) publish 스크립트 재작성: 폴더 상태를 자동 판별한다 —
   (A) 독립 저장소 클론이면 그대로 push, (B) 단독 폴더면 그 자리에서 git init 후
   push(이후 git pull 가능), (C) AI보드 하위 폴더면 임시 복사본으로 push(원본 불변).
   push 성공 시 이후 아이콘 자동갱신이 독립 저장소를 쓰게 된다.

버전 1.7.0 (Python 보드)
업데이트: 바탕화면 아이콘 실행 시 최신 소스 자동 반영.
1) board/auto_update.py 신설 — VERSION.txt 를 원격과 비교해 더 높을 때만 갱신.
   .git 이 있으면(자기 폴더 또는 부모 저장소) git pull, 없으면 GitHub ZIP 에서
   필요한 파일만 덮어쓴다. 독립 repo 우선 → 부모 repo(main) 폴백.
2) run.bat 이 보드 시작 전에 자동갱신을 수행 (--noupdate 로 생략).
3) 사용자 데이터 보호: run-logs · output · .chrome-profile · *.xlsx ·
   .translate_options.json · .site_options.json · *.lnk 는 덮지 않음.

버전 1.6.2 (Python 보드)
업데이트: 바탕화면 아이콘을 작업표시줄에 고정 가능하도록.
1) board/desktop_icon.py — 핀 폴더(User Pinned\TaskBar) 복사 + 셸 「작업 표시줄에
   고정」 verb 호출 + 시작메뉴 등록. 고정 성공/차단 여부를 메시지로 알림.
2) --no-pin 으로 고정 없이 아이콘만 생성 가능.

버전 1.6.1 (Python 보드)
업데이트: 수집사이트 선택 후 [선택조건으로 검색하기] 버튼을 명시적으로 클릭.
(라벨 정확 일치를 1순위 선택자로, 기존 '검색' 폴백은 유지)

이전(1.6.0): P3_필터단위_수집조건수정 — 수집사이트 리스트박스 · 팝업 저장 흐름 · 첫화면 URL.
1) 입력 리스트박스 2개: 수집사이트(select[name=site_id] — 4910.kr /
   ABCmart.a-rt.com / HIVER.co.kr / MUSINSA.com / Zara.com/de) + 번역옵션.
   수집사이트를 고르면 목록 화면에 검색을 걸고 그 결과 행만 처리한다.
2) 실제 흐름대로 재구현: 행의 [수집조건수정](modify_filter(fuid)) → 팝업창
   (/mall/admin/admin_group_modify.php?ps_mode=modify_filter&ps_fuid=…) →
   번역 후 저장 선택 → 하단 [저장하기](set_save()) → 알림 자동확인 →
   팝업(window.close()) 닫힘. 목록 페이지를 떠나지 않아 검색조건이 풀리지 않는다.
3) 첫 화면 URL 기본값: https://tmg1898.cafe24.com/mall/admin/shop/getGoodsCategory.php
4) 폴백: 팝업 클릭 실패 시 팝업 URL 직접 오픈, 저장 버튼 미검출 시 set_save() 호출.

이전(1.5.2): 요건에 없던 보드 [바탕화면 아이콘 만들기] 버튼 제거.
이전(1.5.1): 바탕화면 아이콘은 실행파일 1개만 (실제 활성 바탕화면).
이전(1.5.0): P3_필터단위_수집조건수정 신설 — 번역옵션 리스트박스.
이전(1.4.3): 망고보드를 부모 저장소 main 으로 가져옴. AI보드와 별개의 독립 보드.
