버전 2.46.0 (Python 보드)
업데이트: 11번가·롯데ON 라디오 탭별 검색 규칙 및 3단계 파이프라인 완비.
1) ★요건:
   - A. 입력필드 구분 "국내":
     1. 해외 또는 해외직구 라디오 선택 후 -> "의류" 검색 후 임의의 리스트로 저장 (ALT+S)
     2. 국내 또는 일반 라디오 선택 후 -> "최종 카테고리명" 입력 검색 후 저장 (ALT+S)
   - B. 입력필드 구분 "해외":
     1. 해외 또는 해외직구 라디오 선택 후 -> "해외명품" 검색 후 임의의 리스트로 저장 (ALT+S)
     2. 국내 또는 일반 라디오 선택 후 -> "최종 카테고리명" 입력 검색 후 저장 (ALT+S)
2) `map_categories.py`:
   - 11번가(`11ST`), 롯데ON(`LTON`)에 대해 라디오 순서(1.해외탭 -> 2.국내탭) 및 탭별 지정 검색어("의류"/"해외명품" vs "최종카테고리명") 매핑 분기 적용.
   - 3단계 재확인·재등록 파이프라인 연동.
3) 전체 테스트 484개 통과.

버전 2.45.0 (Python 보드)
업데이트: 엑셀 확정 카테고리명(공백 구분) 단일 1회 조회 및 완전일치 매핑 원칙 재확립.
1) ★요건: "최종카테고리명을 한칸씩 띄어서 입력하고, 망고 조회는 등록 시 1회만 수행할 것. 엑셀과 망고는 100% 동일하므로 확정한 값 그대로 검색하여 완전일치 항목 선택."
2) `map_categories.py`:
   - `search_keyword_for()`: 엑셀에서 확정된 전체 카테고리명을 공백 한 글자씩 띄어 검색어로 구성.
   - `_map_once()`: 구성된 최종 카테고리명으로 망고 검색창에 1회만 단일 조회.
   - `pick_option()`: 검색 결과 목록에서 확정한 카테고리와 100% 완전일치하는 항목만 선택.
3) 전체 테스트 484개 통과.

버전 2.44.0 (Python 보드)
업데이트: 품목 동의어 및 매핑 규칙 15종 확장 (자켓=재킷, 임부복 배제, 스포츠 허용, 정장=수트, 정장구두=로퍼, 캐쥬얼구두=옥스퍼드, 뮬=블로퍼=캐쥬얼구두, 단화=하이힐/미들/로우, 슬리퍼=슬립온=플립플랍=수영장샌들=비치신발, 모자=야구모자=스포트모자, 비니=머리수건=햇=햇볕가리개, 사파리=햇빛가리개=햇=낚시모자 등).
1) ★요건:
   - 1. 자켓 = 재킷 상호동의어 (어느 한쪽 검색 안 될 시 다른 쪽 검색)
   - 2. 임부복 들어간 카테고리는 매핑에서 제외
   - 3. 스포츠가 들어간 단어는 카테고리 매핑 제외 대상 단어에 포함하지 말 것 (스포츠 허용)
   - 4. 우선순위 적용: 여성의류 > 패션의류 > 패션의류잡화
   - 5. 캐쥬얼, 기타 등 단어는 가장 일반화된 범주로 간주 (일치하는 카테고리 단어가 없을 때 해당 단어로 매핑)
   - 6. 패딩, 점퍼, 패딩점퍼, 야상, 패딩다운, 사파리, 바람막이 등 유사동의어
   - 7. 가디건, 스웨터, 외출복 유사동의어
   - 8. 정장 = 수트 동일어
   - 9. 정장구두 = 로퍼 동의어 / 캐쥬얼구두 = 옥스퍼드 동의어
   - 10. 뮬 = 블로퍼 = 캐쥬얼구두 상호동의어
   - 11. 슬리퍼 = 슬립온 = 플립플랍 = 수영장 샌들 = 비치 신발 동의어
   - 12. 단화 = 하이힐(미들) = 하이힐(로우) = 뮬 = 블로퍼 동의어
   - 13. 모자 = 야구모자 = 스포트모자 = 스포츠모자 동의어
   - 14. 비니 = 머리수건 = 햇 = 햇볕가리개 = 햇빛가리개 유사동의어
   - 15. 사파리 = 햇빛가리개 = 햇 = 낚시모자 유사동의어
2) `matching.py`:
   - `ITEM_SYNONYMS` 및 `expand_synonyms()` 15종 동의어 관계 완벽 연동.
   - `FORBIDDEN_BASE_WORDS` 에서 `스포츠` 제거, `임부복/임부/임산부/마터니티` 추가.
   - `SAFE_FALLBACK_WORDS` 에 `캐쥬얼/캐주얼/캐쥬얼의류/기타잡화/기타소품` 추가.
   - 2단계 하위 검색 시 원문 일치 우선 -> 동의어 순차 탐색 파이프라인 정밀화.
3) 전체 테스트 484개 통과.

버전 2.43.0 (Python 보드)
업데이트: 저장 후 3단계 재검증·재등록 파이프라인 (최초 -> 저장 -> 2차 확인/등록/저장 -> 3차 확인/등록/저장) 완벽 구축 및 옵션 선택 동기화 강화.
1) ★요건: "미매핑이 왜 생기는 거야? 저장후--> 다시 한번 확인하는 로직을 구현해..
   최초 -> 저장
   2차 : 확인 -> 누락 확인 -> 2차 등록 -> 저장
   3차 : 확인 -> 누락 확인 -> 3차 등록 -> 저장 OK?"
2) `map_categories.py`:
   - [1단계 최초 등록 & 저장]: 6개 마켓 매핑 후 1차 저장 (Alt+S)
   - [2단계 재확인 & 2차 등록 & 저장]: 저장 후 실제 반영 상태 확인 -> 누락(미매핑)된 마켓만 2차 등록 -> 2차 저장 (Alt+S)
   - [3단계 재확인 & 3차 등록 & 저장]: 2차 저장 후 다시 반영 상태 확인 -> 여전히 누락된 마켓만 3차 등록 -> 3차 저장 (Alt+S)
   - `choose_option()` 에서 DOM change/input 이벤트 및 hidden 필드(`openmarket_cm_category_<코드>`, `openmarket_cm_category_name_<코드>`) 강제 동기화 JS 스크립트 추가 (`CHOOSE_OPTION_JS`).
   - `mapped_state()` 에서 hidden 필드뿐만 아니라 select 요소의 selectedIndex / option 값까지 교차 검증하여 미매핑 정확도 대폭 향상.
   - 단일 검색어 실패 시 다단계 검색어(전체 경로 -> 리프 -> 하위 -> 중위 -> 품목어)로 옵션 목록을 반드시 찾아내도록 개선.
3) 전체 테스트 469개 통과.

버전 2.42.0 (Python 보드)
업데이트: '매핑 시작' 버튼 클릭 시에만 작업이 실행개시되도록 트리거 로직 정리 (콤보박스 선택 시 자동조회 분리).
1) ★요건: "'매핑시작' 버튼 클릭후에 작업을 실행개시해..... 명확히 확인해서 ...수정해...."
2) `board/app.py`:
   - 콤보박스(사이트명/URL) 항목 선택 시 백그라운드 프로세스가 자동 실행되던 이벤트 바인딩을 제거하고, 사용자가 명시적으로 '매핑 시작' (또는 '행 목록 확인') 버튼을 눌렀을 때만 작업을 개시하도록 확정.
3) 전체 테스트 468개 통과.

버전 2.41.0 (Python 보드)
업데이트: 쿠팡 4단계 일치 시 5단계 카테고리 최적 선정 규칙 적용 (검색어 일치 우선 -> 광범위 단어 우선 -> 첫 번째 선택), 비제품(상품권/이용권) 배제어 추가.
1) ★요건: "쿠팡의 카테고리 매핑시 4단계까지 일치하면 5단계는 임의의 한개 즉, 5단계 단어중
   가장 일반적이고 범주가 넓은 하나 또는 선정이 어려운 경우 가장 첫번째 를 선택할것...
   다만, 쿠팡의 경우, 5단계 카테고리명에 명백히 검색어가 있는 경우 앞선 요건의 임의의 매핑은 적용하지 않음.."
2) `matching.py` 에 `leaf_matches_term()`, `is_general_leaf()`, `GENERAL_LEAF_WORDS` 신설.
3) 쿠팡 5단계 매핑 알고리즘:
   - 5단계 카테고리명에 필터명 고유 검색어가 명백히 포함된 경우 최우선 매핑 (+50점).
   - 5단계 카테고리명에 검색어가 없는 경우 "기타/일반/기본/단품/세트/전체" 등 광범위 단어 우선 (+15점).
   - 선정이 어려운 동점 후보군일 때 원래 엑셀 목록 순서의 첫 번째 항목을 안정적으로 선택.
4) 배제 단어에 "상품권", "이용권" 추가 (실제품 오매핑 방지).
5) 전체 테스트 292개 통과.

버전 2.40.0 (Python 보드)
업데이트: 절대 배제 단어 목록에서 "생활용품" 제외 (생활용품 카테고리 정상 매핑 허용).
1) ★요건: "앞선 요건에서 '생활용품'은 제외 할것...."
2) `matching.py` 의 `FORBIDDEN_BASE_WORDS` 목록에서 "생활용품" 단어를 제외하여,
   생활용품 관련 카테고리가 정상적으로 매핑 후보로 사용될 수 있도록 수정.
3) 전체 테스트 289개 통과.

버전 2.39.0 (Python 보드)
업데이트: 국내/해외 구분 입력필드 신설, 절대 배제어 필터링(필터명 미포함 시 완전 배제),
          미매핑 종료 로직 완전 삭제 및 100% 매핑 완주 보장.
1) ★요건: "1. 마켓별 '미매핑'하고 매핑작업을 종료하는 로직은 완전히 삭제할것
   2. 입력필드에 추가: 국내/해외 구분 (위치: '사이트명' 우측)
   3. 브랜드, 여행, 티켓, 스포츠, 등산, 낚시, 배낭, 유아, 아동, 운동, 여가,
      중성, 혼용, 공용, 유니섹스, 남녀, 골프, 안전, 구기, 꽃, 생필품, 배달음식,
      생활용품, 수입명품, 가구, DIY, PC, 주변기기, 가공식품 등 용어가 들어간
      카테고리에는 매핑을 절대 하지 말것 (단, 망고 필터명에 명백히 있는 단어는 예외)
   4. 입력필드값이 '국내'이면 '해외', '수입명품', '수입' 단어가 들어간 카테고리 완전 배제
   5. 입력필드값이 '해외'인 경우에도 '해외', '수입' 단어 카테고리는 최하위 우선순위 부여(최후의 수단)"
2) GUI (`board/app.py`): "상품수집사이트" 리스트박스 우측에 "구분 [●국내 ○해외]" 라디오버튼 신설 및 CLI(`--region-type`) 연동.
3) 매칭 엔진 (`matching.py`, `map_categories.py`):
   - `FORBIDDEN_BASE_WORDS` 및 `forbidden_words_for()`, `is_forbidden_category()`, `strip_forbidden_categories()` 신설.
   - `OVERSEAS_WORDS` 에 "수입", "수입명품", "병행수입", "직구" 확장 및 `has_overseas()`, `filter_by_region()` 신설.
   - `priority_rank()` 에 `region_type` 인자 반영: 국내 모드 시 해외/수입 카테고리 -1000점 감점 배제, 해외 모드 시 일반 카테고리 우선 및 해외/수입 -500점 감점(최후의 수단).
4) 미매핑 방지 보장 (`map_categories.py`):
   - 마켓별 매칭 실패로 인한 프로세스 중단 로직 완전 삭제.
   - 안전 폴백 강제지정 로직을 보강하여 전 마켓 100% 매핑 완주 보장.
5) 전체 테스트 288개 통과.

버전 2.38.0 (Python 보드)
업데이트: 공통 확장형 연관검색어 DB 구축 및 3단계 파이프라인(엑셀 1차 ->
          확장형DB 2차 -> 매핑DB 3차) 통합 적용.
1) ★요건: "1. 파일로 올려준 *CSV파일을 DB화하고 메모리에 올려, 신속한 매핑이
   가능하도록 할것 2. GITHUB에 올려 관리할것 3. A.마켓별 카테고리 분류엑셀자료,
   B.카테고리매핑DB, C.카테고리매핑_확장형DB를 활용하는 방법, 매핑 진행시
   우선순위 및 적용 절차 (1차: 엑셀 직접검색 -> 2차: 확장형DB 범주/범위 확장 ->
   3차: 매핑DB 범주/범위 확장 -> 엑셀 최종 매핑)"
2) `extended_category_master.csv`(표준 카테고리 14,207건) 및
   `extended_keyword_dictionary.csv`(표준 연관검색어 29,999건) UTF-8 변환
   저장 및 `extended_master_db.py` 모듈 신설.
3) 5단계 폭포수(Waterfall: EX 1단계, SY 2단계, MO 3단계, IN 4단계, AB 5단계)
   구현 및 빠른 로드를 위한 `extended_master_db.json` 단일 캐시 구축(로드 0.09초).
4) `matching.find_category` 및 `map_categories.py` 매핑 파이프라인에
   `ext_db` 를 연동하여 1차(엑셀 직접검색) 실패 시 2차(확장형DB 범주/범위 확장),
   3차(매핑DB 범주/범위 확장)로 이어지는 3단계 순차 확장 탐색 구조 완성.
5) 전체 테스트 283개 통과.

버전 2.37.0 (Python 보드)
업데이트: 우선순위 규칙(요건 재정의 B-9)이 신규 1순위 경로(MasterDB)에서
          전혀 호출되지 않던 구현 누락을 수정.
1) ★요건: "9. 우선순위 적용이 필요한 경우(남성의류>패션의류>패션의류
   잡화, 국내>해외, 성별>골프/낚시/스포츠, 남성>중성=혼용=공용 등)를
   왜 안 지키냐"는 지적 — v2.35.0에서 CSV MasterDB를 1순위로 연결할
   때 `priority_rank()`를 옮겨 담지 않고 "MasterDB 순위 첫 후보"를
   그냥 확정해버리고 있었다(옛 캐스케이드에만 남아 있어 거의 호출 안
   됨). "최신성" 문제가 아니라 새 경로 구현 시 누락된 것.
2) `matching.priority_rank()` 에 빠진 하위규칙 2개 추가: "상위:남성-
   팬츠 > 상위스포츠/팬츠", "성별 > 골프/낚시/스포츠" — 필터명이
   활동을 요청하지 않았는데 후보가 그 활동 전용이면 감점, 요청했으면
   가점. (남성의류>패션의류>패션의류잡화·국내>해외·성별동급규칙은
   기존에 이미 구현돼 있었음을 확인.)
3) `map_categories.best_category_via_master()` — CSV 단계(완전일치>
   유사어>형태소분리>직접포함)별로 후보를 모아, **같은 단계 안에서**
   여러 후보가 있으면 `priority_rank()`로 재정렬해 최고점 후보를
   쓰도록 수정(단계 경계는 그대로 존중 — "엑셀이 100% 우선" 원칙과
   충돌 방지).
4) 전체 테스트 279개 통과.

버전 2.36.0 (Python 보드)
업데이트: "여성-신발-구두" 실사례 재현으로 발견한 4가지 매칭 버그 수정
          (성인/아동 오분류·복합어 계열오판·순위 절단·동점 시 무성별 우선).
1) ★요건: 사용자가 "카테고리가 '여성 구두/부츠' 면 여기에 90% 매핑
   정보(여성+구두)가 들어 있지 않냐"고 지적 — 실제로는 6개 마켓
   전부 "유아동 여아구두"·무성별 "신발" 등으로 완전히 새고 있었다.
   원인을 끝까지 추적해 다음 4가지를 모두 고쳤다.
2) `matching.CHILD_WORDS`/`mentions_child`/`is_child_category` 신설 —
   "성별"과 "성인/아동"은 서로 다른 축인데 기존엔 "여아"를 GENDER_
   WORDS["여성"]에 넣어 성별 배제만 해결하고 성인·아동 구분은 못
   했다. 필터명이 아동을 언급하지 않았는데 후보가 아동 카테고리
   ("유아동신발 > 여아구두")면 절대규칙으로 배제하도록 별도 축을
   추가했다(OPPOSITE 에 "아동"을 넣었던 것은 되돌림 — 성별과
   섞이면 오히려 "여아"가 "여성"과 동일 성별로 인식돼 무력화됨).
3) `matching.class_of` — "정장구두" 처럼 수식어(정장=의류)가 머리
   명사(구두=신발)보다 앞에 오는 한국어 복합어를 "가장 먼저 걸리는
   단어" 규칙 때문에 전부 "의류"로 오분류해, "여성화 > 하이힐/
   펌프스/정장구두"가 신발 계열 검색에서 계열불일치로 배제됐다.
   두 계열 단어가 빈틈없이 이어붙은 복합어는 뒤쪽(머리명사) 계열을
   따르도록 수정.
4) `category_master_db.py` — 1.5)단계(카테고리명 직접포함) 후보를
   `limit=10`으로 자르던 것을 `limit=50`으로 늘림(정답이 13번째라
   잘려서 후보 목록에 아예 없었던 사례 발견). `_direct_name_matches`·
   `_similar_candidates` 둘 다 `gender` 인자를 받아, 글자 유사도가
   동점일 때 필터와 같은 성별이 명시된 경로를 우선하도록 함(성별
   표기가 전혀 없는 밋밋한 대분류 "신발"이 "여성단화"를 이기던
   사고 수정).
5) `map_categories.best_category_via_master` — 검색어를 하위→중위
   순으로 시도할 때, 더 구체적인(앞쪽) 검색어의 4단계 강제지정이
   덜 구체적인(뒤쪽) 검색어의 확실매칭보다 실제로 더 정확한 경우가
   있어(예: "구두" 에 완전일치가 없어도 "여성단화"가 4단계로 걸림)
   검색어 인덱스를 비교해 더 구체적인 쪽을 우선하도록 재구성.
6) `_is_non_product_hint` 에 "구두주걱"·"신발주걱"(신발 도구) 추가 —
   "구두" 검색이 이 도구로 새던 것을 막음.
7) 결과(6개 마켓, "여성-신발-구두"): 옥션2.0→여성단화, 11번가→
   여성신발, G마켓2.0→여성 구두/부츠, 스마트스토어→여성신발, 쿠팡→
   하이힐/펌프스/정장구두, 롯데ON→여성단화. 전부 성별·연령 정확,
   6개 중 5개가 "구두" 검색어로 직접 확정(1개만 상위어 "신발"로
   4단계 강제지정).

버전 2.35.0 (Python 보드)
업데이트: 사용자 제공 CATEGORY_MASTER+KEYWORD_DICTIONARY CSV를 1순위
          마스터DB로 연결 — 기존 정적 사전 대신 실데이터가 우선한다.
1) ★요건: "기존 DB를 대체하여 지금 주는 걸 활용하는 프로그램을 구현해.
   DB는 CSV 자료 그대로 DB화하고 GitHub에 올려서 메모리로 보관하여
   속도를 높이도록 해" — 사용자가 업로드한 CATEGORY_MASTER(17,591건·
   6개 마켓)·KEYWORD_DICTIONARY(25,399건, EX=완전일치·SY=유의어·
   MO=형태소분리) CSV를 UTF-8로 변환해 `data/category_master.csv`·
   `data/keyword_dictionary.csv` 로 저장했다.
2) 신설 `category_master_db.py` — CSV를 그대로 읽어 마켓별 카테고리
   트리(Parent_ID)·검색어 사전을 메모리에 올린다. 매번 CSV(약 43,000
   줄)를 재파싱하지 않도록 `data/category_master_db.json` 단일 캐시로
   저장·재사용한다(로드 0.07~0.12초).
3) `resolve_ranked()` — Priority(1=EX·2=SY·3=MO) 순으로 여러 순위의
   후보를 반환해, 상위 호출부가 성별·브랜드 절대규칙으로 앞 후보를
   건너뛰고 다음 후보를 계속 시도할 수 있게 했다. "미검출 절대 금지"
   원칙에 따라 그 마켓에 카테고리가 있으면 최근접 강제지정(4단계)으로
   반드시 하나를 확정한다.
4) `map_categories.best_category_via_master()` — 이 마스터DB를
   **1순위**로 쓰고, 확실한 매칭이 없을 때만 기존 matching.py
   캐스케이드(2·3순위: category_db·keyword_dictionary)로 넘어가도록
   `_map_once`·`map_one_market`·`map_one_row`·`run_mapping`·`run_dry`
   전체를 연결했다. 성별·브랜드는 절대규칙(반대 성별/브랜드만 있으면
   "미검출"로 두고 다음 단계로 넘김), 건강용품·계열불일치는 소프트
   규칙(대안 없으면 그거라도 사용)으로 구현했다.
5) 실사례 감사로 발견한 버그 2건 추가 수정:
   a) `_is_non_product_hint` 가 리프만 보아 "건강/의료용품 > 실버용품 >
      휠체어 악세서리"(최상위에만 "건강"이 있음) 를 놓쳤다 — 경로 전체
      텍스트를 보도록 수정.
   b) `GENDER_WORDS` 에 "남아"·"여아"(아이) 가 없어 "여성-신발-구두"
      검색이 "남아구두"(남자아이 신발)로 새는 사고 — 성별 판정에 추가.

버전 2.34.0 (Python 보드)
업데이트: 실사례 기반 매칭 정확도 버그 4건 수정 + 성능 5배 개선.
1) ★요건: "매핑이 개판이다, AI자동매핑보다 확률이 떨어진다" 지적에 따라
   실제 6개 마켓 캐시 데이터로 12개 필터명을 직접 돌려 정성 감사
   (proactive audit)를 수행 — 다음 4가지 실제 버그를 발견·수정했다.
   a) `path_class` 가 최상위 단계(대분류) 텍스트까지 계열 판정에
      써서, "의류/언더웨어 > 남성언더웨어 > 드로즈/트렁크" 가 최상위의
      "의류" 글자 때문에 "의류" 계열로 잘못 분류돼 "속옷" 검색에서
      "남성화"(신발)로 새는 사고가 났다 — 최상위를 뺀 나머지로 먼저
      판정하도록 수정.
   b) `gender_paths` 가 같은 성별 표기 후보가 있으면 성별 표기가 아예
      없는(반대 성별도 아닌) 후보를 통째로 배제해, "남성-신발-
      스니커즈" 가 정확한 무표기 리프("스니커즈") 대신 성별은 맞지만
      품목이 다른 형제("남성샌들 > 아쿠아슈즈")를 고르는 사고가 났다
      — 반대 성별만 배제하도록 수정, 같은 성별 우선은 `priority_rank`
      점수로 처리.
   c) CLASS_WORDS·ITEM_SYNONYMS 어휘 보강 — "언더웨어·드로즈·팬티·
      트렁크"(속옷 계열), "구두"↔"단화"(마켓 표기상 "구두"는 주로
      남성쪽, 여성 동일 개념은 "단화"로 불림) 용어차 연결.
   d) `NON_PRODUCT_HINT_WORDS` 신설 — "건강목걸이"(건강용품)·"구두약"
      (관리용품) 처럼 품목명을 담고 있지만 실제로는 그 품목이 아닌
      카테고리를 대안이 있으면 고르지 않는다.
2) ★성능: "시간 질질 끄냐" 지적에 실측했더니 `find_category` 1회에
   마켓당 평균 0.5초(6개 마켓·행마다 약 3초)가 걸렸다. 원인은
   (a) `keyword_dictionary.KeywordDB.leaves()`/`is_leaf()` 가 카테고리
   마다 전체를 다시 훑는 O(N²) — 14,000여 건 기준 `resolve()` 1회에
   4초, (b) `class_of` 가 후보마다 계열 단어 전부를 매번 다시
   `normalize` , (c) `_specific_item_conflict` 가 경로마다 같은 값을
   반복 정규화, (d) `expand_synonyms([parsed.mid, *parsed.lows])` 가
   경로마다(항상 같은 값인데도) 다시 계산됨. 부모→자식 색인·정적
   단어 사전 사전정규화·호출부에서 1회 계산 후 재사용으로 고쳐
   **마켓당 평균 500ms → 100ms(5배)**, `keyword_db` 구축은
   4.3초 → 0.36초로 줄었다. 성능 회귀 방지 테스트 3건 추가.

버전 2.33.0 (Python 보드)
업데이트: 연관검색어DB(keyword_dictionary) 실제 실행 경로 연결 +
          "정식 분류" 우선 매칭 버그 수정.
1) ★요건: "연관검색어DB는 만들어만 놓고 전혀 활용 안 하냐" — 지적대로
   `keyword_dictionary.KeywordDB` 가 지금까지 자체 테스트에만 쓰이고
   실제 매핑 실행 경로(`map_categories.py`)에 전혀 연결돼 있지 않았다.
   `matching.find_category` 에 `keyword_db` 매개변수를 추가해 5)단계
   (정보화DB 연관검색어 확장)에서 `category_db` 와 함께 쓰도록 연결하고,
   `map_categories.py` 전체(`best_category*`·`map_one_market`·
   `_map_once`·`map_one_row`·`run_mapping`·`run_dry`)에 실제로 스레딩.
2) ★실사례 버그 수정: "아름트리-무신사-여성-상의-반소매 티셔츠" 필터가
   "여성의류 > 마담의류 > 티셔츠"(무관한 이름의 중위 아래 우연히 걸린
   리프)를 골라 "여성의류 > 티셔츠 > 라운드넥티셔츠"(검색어 자체가
   중위명인, 정식 분류)를 놓쳤다. `_matches_canonical_group()` 신설 —
   검색어(하위)가 리프가 아니라 그보다 상위 단계 이름 자체와 일치하면
   가산점(우선순위 +6)을 준다. 단, 방향은 "검색어가 그 레벨명에 포함"
   쪽만 인정한다 — 반대 방향까지 허용하면 "셔츠"(레벨)가 "티셔츠"
   (검색어)의 부분 문자열이라는 이유만으로 부당하게 우대되는 사고가
   재발한다.

버전 2.32.1 (Python 보드)
업데이트: [AI 자동 매핑 시작하기] 버튼 클릭 재차단 (오해석 원복).
1) 요건재정의(2026-08-22 C-3) 원문 "AI자동매칭 시작하기 버튼클릭
   ****당분간 로직 수행 않함****" 을 "클릭은 하되 결과에 로직을 얹지
   않는다"로 잘못 해석해 v2.29.0 에서 `click_ai_mapping()` 호출을 다시
   켰던 것을 원복한다 — 실제 의도는 "이 단계(클릭) 자체를 당분간
   수행하지 않음"이었다. 사용자가 명시적으로 재확인·지적함.
2) v2.27.0 에서 처음 끈 이유(버튼이 먼저 채운 값이 우리 검색 실패 시
   그대로 저장될 위험)가 여전히 유효하므로 다시 주석 처리했다. 마켓별
   매핑은 이 버튼과 무관하게 항상 엑셀 확정값으로만 검색·선택한다.

버전 2.32.0 (Python 보드)
업데이트: 마켓별 카테고리 JSON 캐시 신설 — 엑셀 없이도 재사용 가능.
1) 신설 `market_cache.py` — 마켓별 카테고리 경로(엑셀을 읽은 원본 결과)를
   `data/market_categories_cache.json` 에 저장·로드. 파생 결과(색인·
   동의어 사전)는 캐시하지 않고 이 원본에서 항상 다시 계산한다(사전이
   나중에 바뀌어도 캐시가 낡은 값을 주지 않도록).
2) ★요건: "입력에 엑셀 파일이 있으면 그걸 최신성으로 간주하고 DB를
   갱신, 없으면 DB(캐시)를 최신 자료로 보고 매핑을 수행" — `load_market_
   excels()` 가 엑셀을 성공적으로 읽으면 캐시를 자동 갱신하고,
   `load_market_excels_or_cache()` 가 엑셀 경로가 없거나 읽기 실패하면
   캐시로 대체한다. `run_mapping`·CLI(`main`) 양쪽에 연결.
3) 실제 6개 마켓 엑셀(14,882개 카테고리 경로, 964KB)로 캐시를 구축해
   `data/market_categories_cache.json` 으로 GitHub에 커밋 — 다음부터는
   엑셀 파일이 없어도 이 캐시로 매핑 작업을 계속할 수 있다.

버전 2.31.0 (Python 보드)
업데이트: 연관검색어DB — 범위 한정(scoped) 대체 + "반드시 하나" 강제지정.
1) 요건: "연관검색어DB 구축 관련 요건 반영 우선순위는 최신성을 최우선으로
   할 것" — `category_db.py`(교차색인)와 `keyword_dictionary.py`(ERD)가
   충돌하면 더 최근에 만든 `keyword_dictionary.py` 를 공식 기준으로 삼는다.
2) 신설 `substitute_for(cat_id, available_cat_ids=…)` — 특정 카테고리
   하나가 "이 마켓엔 없다"고 가정하고, 그 카테고리만 기준으로 형제(3단계)
   → 조상(4단계, 조상 자신이 없으면 그 하위트리/사촌까지) 순으로만 대체를
   찾는 **범위 한정** 조회. 기존 `resolve()`(전역 조회)에서는 RE(형제)가
   항상 SY 뒤에 가려 실제로 선택되지 않던 한계를 해결한다.
3) ★요건(절대): "형제·상위까지 없다고 미검출로 끝내는 건 이 DB를 만든
   목적에 위배된다" — 5) 최근접 강제지정 단계를 추가해, `available_cat_ids`
   에 카테고리가 하나라도 있으면 그 범위 전체에서 이름이 가장 비슷한
   것을 반드시 하나 확정한다. `None` 은 그 범위 자체가 완전히 빈 목록일
   때만 나오고, 이때도 "미검출"이 아니라 "카테고리 자료 없음"으로 구분한다.
4) `resolve_with_fallback(keyword, available_cat_ids=…)` — 1)완전일치→
   2)유사어→[대상이 범위 밖이면] 3)형제→4)조상→5)최근접강제지정 전체
   흐름을 하나로 묶은 진입점. 요건 원문의 "캡모자 → M112 → 형제 → 상위"
   시나리오를 그대로 재현·검증.
5) `matching.py` — `find_category` 5)단계(정보화DB) 검색어 확장 정밀화.
   연관검색어를 그대로 넣으면(예: "야구모자/뉴에라/스냅백") `level_hit`
   의 양방향 부분일치 때문에 흔한 말("모자")이 다시 섞여 무관한 후보까지
   쓸어담는 문제를 발견 — 1순위(하위) 연관어만, 반쪽분해(halves) 없는
   단순 토큰화로 한정해 확장하도록 좁혔다.

버전 2.30.0 (Python 보드)
업데이트: 연관검색어DB(CATEGORY_MASTER + KEYWORD_DICTIONARY) 신설.
1) 신설 `keyword_dictionary.py` — 제시된 ERD(카테고리 마스터·키워드 매핑
   사전) 그대로 구현. `CATEGORY_MASTER(Cat_ID·Cat_Name·Parent_ID·Level·
   Full_Path)` + `KEYWORD_DICTIONARY(Keyword_ID·Search_Keyword·
   Target_Cat_ID·Mapping_Type·Priority)`.
2) 리프마다 SY(동의어)·MO(형태소분리, 성별 접두어 양방향 결합)·
   RE(동일범주/형제어)·EX(확대범주/상위어)를 자동 생성. 기존
   `matching.ITEM_SYNONYMS` 사전도 SY 로 병합.
3) `resolve()` — 요건 4단계 폭포수(완전일치→유사어→동일범주→확대범주)로
   검색어 하나를 카테고리로 확정.
4) 스크린샷 샘플(계층형 카테고리 87건)로 실제 구축 — CATEGORY_MASTER
   115건 · KEYWORD_DICTIONARY 약 980건, 요건 예시(캡모자·뉴에라·무스탕·
   로퍼·정장바지) 전부 재현 확인.
5) `category_db.py`(마켓 교차색인)와 보완 관계 — 필요시 `find_category`
   5) 단계에 함께 연결 가능.

버전 2.29.0 (Python 보드)
업데이트: 카테고리 매칭 요건 전면 재정의 — 통합정보화DB 신설 + 탐색순서 재작성.
1) 신설 `category_db.py` — 6개 마켓 엑셀을 하위·중위·상위 단계로 교차색인해
   연관검색어(하위=1순위·중위/상위=2·3순위)를 관리하는 통합정보화DB.
2) `matching.find_category` 탐색순서를 요건 그대로 재작성:
   1) 완전일치 → 2) 하위검색(count 0/1/>1 분기) → 3) 우선순위(상위·중위
   일치 → 국내우선 → 정보화DB) → 4) 확장범주 → 5) 정보화DB 연관검색어로
   확장(2~5 최대 3회 반복) → 6) 근접매핑 강제지정. 절대규칙(성별 반대·
   브랜드·엑셀범위)은 그대로 유지.
3) 우선순위 9가지 반영 — <성별>의류 > 패션의류 > 패션의류잡화, 국내 > 해외,
   성별 정확일치 > 중성/혼용/공용 (`priority_rank`).
4) 규칙3(품목명 일치) 우선순위를 상위→중위→하위에서 **하위→중위→상위**로
   반전 — 리프(가장 구체적인 단계)에 가까운 후보를 먼저 고른다.
5) 엄격규칙: 선글라스 ↔ 시계 상호배타(단 "시계/잡화" 결합 표기는 예외).
6) 중위도 하위처럼 "/" 다중조각으로 분해(`segment_variants`) — "모자/잡화"
   → 모자·잡화·모자/잡화.
7) `map_categories.py` — 실행 시작 시 통합정보화DB를 1회 구축해 매칭 전체
   경로에 전달, [AI 자동 매핑 시작하기] 클릭 재활성화(결과에 로직은 얹지
   않고 실패 시 `clear_market_category` 안전장치로 계속 보호).

버전 2.28.0 (Python 보드)
업데이트: 리프에 무관 단어가 섞이면 관련 품목까지 잘못 배제되던 버그 수정.
1) 실사례(옥션2.0): 필터 "남성-신발-샌들" 이 "신발 > 남성샌들 > 정장샌들"
   대신 "아쿠아슈즈"를 골랐다. "정장샌들" 리프에 "정장"(의류 단어)이
   같이 있어서, 형제품목 배제 검사가 "샌들"(관련 단어)이 있는데도 그냥
   "정장" 때문에 통째로 배제해버렸다.
2) _specific_item_conflict 수정 — 리프에 찾는 것과 관련된 단어가 하나라도
   있으면, 무관한 단어가 같이 있어도 형제 충돌로 보지 않는다.

버전 2.27.0 (Python 보드)
업데이트: 망고 접촉 마켓당 1회 고정 · 검증 안 된 값은 저장 전 비움 · 형제품목
           배제는 소프트(가장 비슷한 거라도 전달), 브랜드·성별은 절대규칙 유지.
1) 요건: "엑셀에서는 10번이던 100번이던 마음대로 하고, 망고에서는 데이터
   입력만 해(검색 1번만 허용)". map_one_market 이 실패 시 다른 카테고리로
   최대 3회 망고를 다시 검색하던 루프를 제거 — _map_once 를 정확히 한 번만
   호출하는 얇은 래퍼다.
2) 원인 규명 — "브랜드..." 오매핑은 예전(정리 전) 엑셀 + 예전(v2.12.0 이전
   유사도매칭) 코드로 이 3회 재시도 루프가 골라 저장한 값이었다(지금 6개
   실제 엑셀엔 해당 문구가 없음을 직접 검증). [AI 자동 매핑 시작하기] 는
   그 값의 근본 원인은 아니었던 것으로 보이지만, 이 버튼이 필드를 먼저
   채운 뒤 우리 검색이 실패하면 "필드는 그대로"→"검증 안 된 값이 그대로
   저장" 이 되므로, 3)의 안전장치(clear_market_category)를 넣기 전까지
   호출을 비활성화해 둔다(소스는 남김).
3) ★새 안전장치 — 검색이 실패하면(완전일치 없음 등) 그 마켓의 hidden
   카테고리 필드를 명시적으로 비운다(clear_market_category). [AI 자동
   매핑]이나 이전 실행이 채워둔, 우리가 검증하지 못한 값이 [저장] 클릭
   순간 그대로 저장되는 사고를 막는다.
4) 요건: "엑셀에서는 반드시 최종 카테고리명을 망고로 전달해 — 가장 비슷한
   거라도 전달해". 동떨어진 형제 품목(신발→구두 등) 배제를 **소프트**로
   바꿔, 안전한 후보가 없을 때는 포기하지 않고 형제 품목이라도 전달한다.
   성별·브랜드 배제는 예외 없는 절대규칙으로 유지(가장 비슷한 것이라도
   이 둘을 넘지 않는다).
5) 코드 전체에서 "검색어 입력 → 검색 → 목록선택" 흐름은 _map_once 안에
   한 곳뿐임을 확인함(중복 로직 없음).

버전 2.25.0 (Python 보드)
업데이트: "브랜드" 카테고리는 엑셀에 있어도 절대 확정하지 않는다.
1) 실사례: v2.24.0(완전일치·전체이름검색) 적용 후에도 옥션2.0·11번가·
   G마켓2.0 에서 "브랜드 여성의류 > 야상/점퍼/패딩 > 바람막이" 류가 그대로
   매핑됐다 — 완전일치 로직은 정상 동작했고, 실제로 그 문구가 해당
   마켓들의 엑셀 안에 있었다(브랜드관 전용 트리가 정식 카테고리와 별도로
   존재). "엑셀에 있으면 무조건 정답" 이 아니라, "브랜드" 가 붙은
   카테고리는 애초에 확정 후보에서 제외해야 했다.
2) find_category 최상단(성별·형제품목 배제와 같은 위치)에서 "브랜드"가
   붙은 후보를 제외 — 절대규칙. 정식(비브랜드) 카테고리가 있으면 그걸
   쓰고, 브랜드 카테고리만 있으면 매핑하지 않는다.
3) 판단은 여전히 엑셀 검색 단계(matching.py)에만 둔다 — 망고 쪽
   (map_categories.py)에는 손대지 않았다.

버전 2.24.0 (Python 보드)
업데이트: 망고 검색어 — 단계 사이를 공백 한 글자로 구분.
1) 요건: 상위+중위+하위+세부+상세 카테고리를 묶어 검색할 때 각 단계 사이에
   공백을 한 글자씩 둔다. 예) "남자-하의-팬츠-한무-두모" -> "남자 하의 팬츠
   한무 두모". 내부 표기의 " > " 구분자를 공백 하나로 바꿔 검색어를 만든다.

버전 2.23.0 (Python 보드)
업데이트: 망고 쪽 판단 로직 전면 제거 — 확정된 카테고리명을 그대로 제출.
1) 요건 원문: "리스트에서 동일한 것을 선택해 (다른 로직을 구사하지 말고)
   오직 [엑셀에서] 확정한 것만 선택하라". 성별·계열·형제품목·리프일치 같은
   판단은 엑셀 검색(matching.find_category)에서 전부 끝난다. 망고 쪽
   (map_categories.py)은 그 확정값을 그대로 검색·선택하는 기계적 동작만
   한다 — 추가 판단 로직을 두지 않는다.
2) pick_option 을 완전일치(공백 무시) 하나만 남기고 리프일치·유사도·
   filter_name 기반 성별 사전필터를 전부 제거. 완전히 같은 게 없으면
   매핑하지 않는다.
3) ★search_keyword_for 가 리프(마지막 단계) 대신 확정된 카테고리명 전체를
   검색어로 쓴다. 엑셀은 망고 마켓별 전체 카테고리를 그대로 내려받은
   것이므로, 확정값은 망고에도 항상 있다. 리프 하나로만 검색하면 같은
   리프를 쓰는 다른 카테고리(다른 성별 등)까지 걸려 여러 개가 나오지만,
   전체 이름으로 검색하면 그 마켓 안에서 유일하게 하나만 걸린다.
4) _map_once 의 사후 성별 재검사·EXCLUDED_UPPER_WORDS("브랜드") 특례를
   제거 — 완전일치 하나만 쓰면 이런 예외 규칙이 필요 없다(자동으로 걸러짐).

버전 2.22.0 (Python 보드)
업데이트: "브랜드" 등 엑셀에서 지운 말이 리프일치로 새는 것 차단 · 형제품목규칙
           전 단계 적용 · 오탐 2건 수정.
1) 실사례: 옥션2.0="브랜드 캐쥬얼의류 > 티셔츠/셔츠 > 맨투맨/후드",
   11번가="브랜드 여성의류 > 점퍼 > 패딩/다운점퍼" — 엑셀엔 "브랜드" 가
   전혀 없는데(사용자가 지웠음) 리프만 같다는 이유로 그대로 선택돼
   화면에 반영됐다. pick_option 에 EXCLUDED_UPPER_WORDS("브랜드") 검사를
   추가 — 엑셀 확정값에 없는 이 말이 붙은 옵션은 리프가 같아도 고르지 않음.
2) v2.20.0 의 "동떨어진 형제 품목 금지" 를 3) 최근접 단계뿐 아니라 1)~2-4)
   모든 단계의 후보 풀에 똑같이 적용(find_category 최상단에서 all_paths
   자체를 좁힘). 실사례: "맨투맨/후드" 필터가 "패딩"(형제 품목)으로 새던
   문제 — 그 예시가 최근접 단계가 아니라 2-1) 단계에서 뽑히고 있었다.
3) 오탐 2건 수정 — (a) path_hit 의 양방향 substring 이 "상의"가 "여성속옷
   상의" 안에 들었다고 반대방향으로 오판해 정상 후보를 형제품목으로 잘못
   배제 (b) 상위 계층 그룹명("속옷/홈웨어"의 "홈웨어")까지 검사해 정상
   후보를 잘못 배제 — 이제 리프(마지막 단계)만 보고 단방향 포함만 검사.

버전 2.21.0 (Python 보드)
업데이트: 망고 검색은 확정 카테고리명으로 딱 한 번만 — 하위→중위 재검색 제거.
1) 요건: 매핑은 엑셀에서 끝낸다. "최종 카테고리명"만 망고에서 검색·반영한다.
   v2.14.0 에서 넣은 망고 화면 하위→중위 재검색은 잘못 짚은 것이었다 —
   그 요건은 엑셀 안에서의 검색(matching.find_category, 원래부터 구현돼
   있었음) 순서를 말한 것이었는데 망고 화면 검색에 적용했다.
2) search_terms_for 제거. _map_once 가 다시 검색어 1개(리프)로 한 번만
   검색하고, 나온 결과를 그대로 반영한다(pick_option 의 완전일치·리프일치
   기준은 그대로).

버전 2.20.0 (Python 보드)
업데이트: 동떨어진 형제 품목으로는 절대 매핑하지 않는다 (DB화).
1) 요건: 신발→구두(NOT-OK) 신발→잡화(OK) · 양말→티셔츠(NOT-OK)
   양말→기타의류(OK) · 남성스니커즈→여성스니커즈(NOT-OK)
   남성스니커즈→남성신발(OK). "하위에서 없으면 중위로" 물러나는 건 맞지만,
   동떨어진 형제 품목으로 새면 안 된다.
2) SPECIFIC_ITEM_WORDS(클래스별 구체적 하위 품목명) · SAFE_FALLBACK_WORDS
   (안전한 일반화어) 를 신설해 DB화했다. nearest_category(3단계 최근접)가
   이 DB를 보고, 찾는 품목과 다른 형제 품목은 후보에서 제외한다.
3) 성별 규칙과 동일한 절대규칙 — 안전한 후보가 하나도 없으면 매핑하지
   않는다(빈 값). "반드시 하나는 고른다"는 기존 원칙보다 "동떨어진 매핑
   금지" 가 우선한다.

버전 2.19.0 (Python 보드)
업데이트: "모자" 하위 카테고리(다른 이름) 추가.
1) 야구모자·헌팅캡·베레모·페도라·사파리햇·트루퍼·바라클라바·기타모자를
   모자 계열에 추가. 여성 원피스/스커트·가방·소품 은 화면 캡처 대기 중.

버전 2.18.0 (Python 보드)
업데이트: 바지·속옷/홈웨어·신발(품목별) 하위 카테고리(다른 이름) 추가.
1) 바지 하위: 데님팬츠·트레이닝팬츠·조거팬츠·코튼팬츠·슈트팬츠·슬랙스·
   숏팬츠·레깅스·점프슈트·오버올·기타하의 — 의류 계열에 추가.
2) "속옷" 계열 신설 — 속옷·홈웨어·여성속옷상의·여성속옷하의·여성속옷세트.
3) 신발 품목별: 신발용품·패딩/퍼 신발 계열 추가.
4) ★class_of 동시 위치 판정 규칙 보강 — "패딩/퍼 신발" 처럼 의류 단어
   ("패딩")와 신발 단어("신발")가 한 문구에 같이 있으면, 더 길게(구체적으로)
   일치하는 쪽을 우선한다. 이전에는 먼저 발견된(더 짧은) 단어가 항상 이겨서
   "패딩/퍼 신발"이 잘못 의류로 분류됐다.
5) 여성 원피스/스커트·가방·모자·소품 은 화면 캡처 대기 중 — 아직 반영 안 함.

버전 2.17.0 (Python 보드)
업데이트: "아우터" 하위 카테고리(다른 이름)를 의류 계열로 인식.
1) OUTER_SUBTYPES 신설 — 후드집업·블루종·라이더스·슈트·블레이저·카디건·
   경량패딩·패딩베스트·패딩·사파리·헌팅재킷·트러커재킷·스타디움재킷·
   코치재킷·트레이닝재킷·아노락·플리스·뽀글이·환절기코트·베스트·무스탕·
   싱글코트·더블코트·숏패딩·롱패딩·헤비아우터.
2) CLOTHING_WORDS·CLASS_WORDS[의류] 양쪽에 동일하게 반영 — 필터명에 이
   중 하나만 있어도 의류 계열로 인식해 신발·뷰티 등 다른 계열과 안 섞인다.

버전 2.16.0 (Python 보드)
업데이트: "뷰티" 품목 계열 추가 — 하위 카테고리명(다른 이름)도 뷰티로 인식.
1) CLASS_WORDS 에 "뷰티" 신설 — 스킨케어·마스크팩·베이스메이크업·립메이크업·
   아이메이크업·네일·프레그런스·선케어·클렌징·필링·헤어케어·바디케어·
   쉐이빙·제모·뷰티디바이스·미용소품·헬스·푸드·화장품·코스메틱.
2) 필터명에 이 중 하나만 있어도 "뷰티" 계열로 인식해, 기존 품목 계열 배타
   규칙(strip_other_classes)에 따라 의류·신발 등 다른 계열과 섞이지 않는다.

버전 2.15.0 (Python 보드)
업데이트: 짧은 필터명("성별-상위-하위" 3조각) 파싱 버그 수정.
1) 원인 — 필터명이 "여성-신발-구두"·"남성-잡화-신발" 처럼 브랜드-사이트
   없이 3조각뿐이면, 앞 2조각을 무조건 브랜드-사이트로 무시하던 로직이
   성별·상위 정보("여성","신발" 등)를 통째로 날려버렸다. 그 결과 "구두"
   하나만 top 으로 남고 성별·계열 제약이 힘을 잃어 완전히 다른 카테고리
   (옷 등)가 선택됐다.
2) 조각 수가 충분할 때만(성별 뒤에 최소 상위 하나가 남을 만큼, 즉 4조각
   초과) 앞 2조각을 브랜드-사이트로 무시하도록 수정. 5조각 이상인 정상
   필터명(기존 동작)은 그대로다.
3) 신발 동의어에 활동화·스포츠화·워커 추가.
4) 요건에서 준 9단계 검색 순서(남성신발→잡화신발→신발안남성→...→동의어)는
   기존 find_category 의 단계별 대조(상위→중위→하위, 하위전체, 중위전체,
   품목포괄, 최근접)로 이미 동등하게 구현돼 있음을 실제 사례로 재현·확인.

버전 2.14.0 (Python 보드)
업데이트: 망고 검색 — 확정된 카테고리명으로 하위→중위 순서로 재검색.
1) 요건: 엑셀에서 최종 확정한 카테고리명은 바꾸지 않는다. 그 확정값으로
   망고를 검색할 때 1) 하위(리프) 검색어로 먼저 시도하고, 결과가 없으면
   2) 중위(한 단계 위) 검색어로 재시도한다. 검색어가 바뀌어도 결과에서
   고르는 기준(완전일치·리프일치, v2.12.0 에서 확정한 그대로)은 전혀
   바꾸지 않는다 — 검색어 재시도 로직과 최종 선택 기준을 분리했다.
2) search_terms_for 신설. _map_once 가 이 순서로 검색을 재시도하도록 수정.
3) 부가 수정 — read_result_options 의 timeout_ms 기본값이 함수 정의
   시점의 T_LIST 값으로 고정돼 있어 테스트에서 monkeypatch 로 줄여도
   반영되지 않던 결함을 고쳤다 (테스트 속도 25초 -> 1.5초, 운영 동작은
   동일).

버전 2.13.0 (Python 보드)
업데이트: 하단 페이지 로더 감안 — 작업행 번호가 페이지를 넘어가도 이어서 채운다.
1) 요건: 검색 결과가 한 페이지(예: 100행)를 넘고 하단에 페이지 로더
   (pageLoad(N, site)) 가 있을 때, 작업행 범위가 다음 페이지까지 걸치면
   자동으로 그 페이지를 눌러 이어서 채운다.
2) collect_rows_for_range 신설 — 요청 범위가 현재 페이지 안에서 끝나면
   페이지를 넘기지 않는다(기존과 동일한 빠른 경로). 넘을 때만 필요한
   페이지까지만 순서대로 이동한다.
3) P3_설정수정_카테고리매핑초기화·P5_101 동일 적용.

버전 2.12.0 (Python 보드)
업데이트: 엑셀 범위 밖 카테고리를 절대 선택하지 않도록 — 유사도 폴백 제거.
1) 요건: 엑셀="남성 신발" · 망고 목록에는 "브랜드 남성 신발" 처럼 글자가
   겹치는 항목이 있어도, 엑셀 목록에 없는 카테고리는 절대 선택하지 않는다.
2) 원인 — pick_option 의 3단계(최고 유사도, 임계값 없음)가 리프도 안 맞고
   완전일치도 아닌데 글자가 조금이라도 겹치면 그냥 골랐다. 이 3단계를
   완전히 제거했다 — 이제 완전일치 → 리프일치(다단 경로 마지막 단계 정확히
   같을 때만) 두 단계뿐이고, 둘 다 아니면 아예 고르지 않는다(빈 문자열).
3) 표기만 다른 정당한 경우(엑셀="패션의류잡화 > 여성신발 > 로퍼" · 
   망고="패션 > 여성신발 > 로퍼")는 리프("로퍼")가 정확히 같으므로 그대로 허용.
4) 오매핑보다 미매핑이 낫다 — 못 찾으면 그 마켓은 매핑 실패로 처리돼
   기존 미매핑 재검증 로직이 다시 시도한다.

버전 2.11.0 (Python 보드)
업데이트: P5_101 성별 오매칭 잔존 사례 수정 · 저장 후 성별 재검증 추가.
1) 원인 — "임부복" 처럼 글자에 '여성' 이 없는 여성 전용 카테고리가 성별
   판정 단어 목록에 없어 성별무관으로 취급됐다. 마땅한 남성 카테고리가 없을 때
   최근접 단계가 이를 배제하지 않고 그냥 골라 남성 필터에 임부복 이 매핑됐다.
   GENDER_WORDS[여성] 에 임부·임산부·마터니티·maternity 추가.
2) ★요건: [검색필터 설정저장] 후 실제 저장된 값(hidden 필드)을 다시 읽어
   반대 성별로 매핑됐는지 재검증한다 (anomalous_gender_markets 신설).
   이상이 있으면 그 카테고리를 제외하고 재매핑 후 재저장, 최대 3회
   (기존 "미매핑 재검증" 과 동일한 방식). 3회 후에도 남으면 경고 로그+
   detail[gender_anomaly] 기록.

버전 2.10.3 (Python 보드)
업데이트: 팝업 2개가 번갈아 뜨던 문제 — 같은 버튼 중복 클릭 제거.
1) 원인 — expect_popup() 이 타이밍상 팝업 이벤트를 놓치면(실제로는 창이 열렸는데
   감지만 실패) 예외로 처리돼 같은 [설정수정] 버튼을 다시 클릭했다. 그러면 진짜
   팝업이 하나 더 열려 화면에 팝업 2개가 번갈아 보이고, 첫 팝업은 아무도 처리하지
   않은 채 남았다.
2) 클릭 실패로 보일 때 먼저 새로 열린 탭이 있는지 확인해 그걸 쓰고, 없을 때만
   다음 컨텍스트에서 다시 클릭한다. P3_설정수정_카테고리매핑초기화·P5_101
   동일 적용.

버전 2.10.2 (Python 보드)
업데이트: 크롬 창을 화면 앞으로 — 작업 과정(팝업·입력·닫기)이 안 보이던 문제.
1) 원인 — P3_필터_갱신 은 매 단계 bring_to_front() 로 크롬 창을 앞으로 가져와
   실제 진행 과정이 보였는데, P3_설정수정_카테고리매핑초기화·P5_101 은 이 호출이
   전혀 없었다. 실제로는 정상 동작해도 보드 창 뒤에 숨어 사용자에게 안 보였다.
2) 목록 화면 이동 직후·설정수정 팝업이 열린 직후 각각 bring_to_front() 추가
   (reveal 함수 신설, 두 프로그램 동일 적용).
3) 설정수정 버튼도 count() 로 먼저 확인 후 클릭하도록 P5_101 에도 동일 적용
   (P3_설정수정_카테고리매핑초기화 는 v2.10.1 에서 이미 적용).

버전 2.10.1 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 — 행별 설정수정 팝업 헛대기 제거.
1) 원인 — open_setting_popup 이 프레임을 돌 때 [설정수정] 링크가 있는지 먼저
   확인하지 않고 곧바로 클릭+팝업대기를 시도했다. 실제 화면엔 광고/트래킹
   iframe 등 관계없는 프레임이 여러 개 있어, 그 프레임들에서 매번 T_CLICK·
   T_NAV 만큼 헛기다린 뒤에야 진짜 프레임으로 넘어갔다 — 행마다 누적됐다.
2) 이제 프레임마다 count() 로 링크 존재를 먼저 확인(대기 없음)하고, 있는
   프레임에서만 클릭을 시도한다. 초기 목록 화면·검색 단계(_open_mango·
   apply_site_filter·wait_for_rows)는 그대로 두었다.
3) 순서(설정수정 클릭 → 팝업 → 검색필터 설정삭제 → 닫기)는 그대로.

버전 2.10.0 (Python 보드)
업데이트: 사이트명·URL 이력(리스트박스) · 사이트선택 실패해도 목록 URL 결과 유지.
1) P5_101·P3_설정수정_카테고리매핑초기화 — 사이트명·목록(작업)URL 을 콤보박스로
   바꿔 이전 입력을 리스트박스에서 다시 고를 수 있다 (board/input_history.py,
   최근 10개, 각 프로그램 폴더의 .recent_site.json·.recent_url.json).
2) 리스트박스에서 값을 고르면 즉시 그 조건으로 망고 목록을 조회해 보여준다
   ([행 목록 확인] 자동 실행).
3) 사이트 선택/검색이 실패해도 행 수집을 멈추지 않는다 — 붙여넣은 URL 이 이미
   조건을 담고 있어 화면에 결과가 떠 있는 경우가 있어, 여기서 멈추면 있는 행을
   0건으로 오판한다 (P5_101·P3_설정수정_카테고리매핑초기화 공통).
4) P3_필터단위_수집조건수정 의 match_option 이 대소문자를 구분해
   MUSINSA.COM 입력이 MUSINSA.com 옵션을 못 찾던 버그 수정 (그 프로그램 자체는
   그대로 두고 이 함수만 대소문자 무시로 보강 — 기존 정확일치 동작은 그대로 유지).
5) P3_설정수정_카테고리매핑초기화 를 요건대로 P5_101 대신 P3_필터단위_수집조건수정
   (update_collect_option.py) 소스를 재사용하도록 재작성.

버전 2.9.1 (Python 보드)
업데이트: .bat 실행 안 됨 — LF 줄바꿈 수정 (근본 원인).
1) 원인 — 모든 .bat 이 LF 로만 저장돼 있었다. cmd.exe 는 LF 만 있는 .bat 을
   제대로 파싱하지 못해 set "PY=py -3" 이 먹지 않고 %PY%가 빈 값이 됐다.
   그 결과 call %PY% -m pip install ... 이 call -m pip install ... 로 실행돼
   '-m' 은(는) 내부 또는 외부 명령... 오류로 죽었다. 망고보드가 전혀 뜨지
   않은 근본 원인이다.
2) .bat 파일 27개 전부 CRLF 로 변환.
3) .gitattributes 신설 — *.bat·*.cmd 를 CRLF 로 못박아 이후 커밋에서
   다시 LF 로 정규화되지 않게 한다.
4) board/tests/test_bat_line_endings.py 신설 — CRLF·BOM 없음·PY 설정 순서 검증.

버전 2.9.0 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 를 요건대로 재작성 (P3_수집조건수정 기반).
1) 요건 1번 위반을 바로잡았다 — P5_101 이 아니라 P3_필터단위_수집조건수정 소스를
   활용한다. 접속(_open_mango) · 사이트선택(apply_site_filter) · 검색
   ([선택조건으로 검색하기]) · 목록탭 선택(pick_list_page) · 팝업닫기(close_popup)
   를 그 모듈에서 그대로 호출한다. 대기 상수도 그 값을 쓴다.
2) 버튼만 다르다 — [수집조건수정] 자리에 [설정수정](market_mapping_new), 팝업에서
   번역옵션 대신 [검색필터 설정삭제](config_remove).
3) 작업 URL 의 지난 검색조건(pmode=filter_delete&uids= · 당일 날짜 · site_id)이
   결과를 0건으로 만들던 문제 — 1차는 입력 URL 그대로, 0행이면 쿼리를 뗀 목록
   주소로 재시도한다. 요건 0번대로 화면에서 조건을 걸고 검색한다.
4) 검색 결과가 나올 때까지 최대 60초 대기 (나오면 즉시 진행).
5) P5_101 은 성별 오매칭 수정만 남기고 그 외 변경을 전부 원복했다.

버전 2.8.5 (Python 보드)
업데이트: 행 0건 진단 강화 — 화면이 쓰는 함수명·행 식별자를 그대로 출력.
1) attr-uid 개수 · td[id^=td_] 개수 · ps_ftid 링크 개수 · table id 목록 ·
   onclick 함수명 목록을 로그에 남긴다. 추측 없이 폴백을 맞추기 위한 것.
2) 사이트 선택은 정상 동작 확인 (MUSINSA.com).

버전 2.8.3 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 — 작업 URL 을 필수 입력으로.
1) 원인 — 보드 「작업 URL」 칸에 DEFAULT_LIST_URL 을 미리 채워뒀는데 그 기본값이
   admin_group.php?pmode=filter_delete&uids=& 라 목록이 렌더링되지 않았다.
   그대로 실행하면 행 0건으로 끝난다.
2) 이제 기본값을 채우지 않고 [필수] 로 표기한다. 비워두고 실행하면
   '작업 URL 을 입력하세요' 로 즉시 중단한다 — 되돌릴 수 없는 작업이라
   엉뚱한 화면에서 돌지 않게 막는다.
3) 목록 화면 주소창 URL 을 그대로 붙여넣으면 된다.

버전 2.8.2 (Python 보드)
업데이트: 상품수집사이트 선택 실패 수정 · 진단 로그 보강.
1) 원인 — 입력한 사이트명을 옵션과 **정확 일치**로만 찾았다. 그래서
   `MUSINSA.COM` 입력이 화면 옵션 `MUSINSA.com` 을 못 찾고 실패했다.
   이제 정확 → 대소문자·공백 무시 → 부분 포함 순으로 맞춘다
   (P3_필터단위_수집조건수정 의 match_option 과 같은 방식).
   실패 시 화면의 옵션 목록을 로그에 그대로 남긴다.
2) 진단 로그 — URL 을 80자에서 자르지 않고 통째로 남긴다. 목록 표가 아예 없으면
   「작업 URL 이 검색필터 목록 화면이 맞는지」 안내를 덧붙인다.
   상품수집사이트 옵션 목록도 함께 출력한다.
3) P5_101 · P3_설정수정_카테고리매핑초기화 둘 다 같은 select_site 를 쓰므로
   함께 고쳐진다.

버전 2.8.1 (Python 보드)
업데이트: 바탕화면 아이콘을 작업표시줄에 고정 가능하도록.
1) 원인 1 — 바로가기 타깃이 run.bat 이었다. Windows 는 .bat 을 직접 가리키는
   바로가기에 「작업 표시줄에 고정」 을 아예 제공하지 않는다. 이제 타깃을
   cmd.exe 로 두고 run.bat 을 인자(/c "…\run.bat")로 넘겨 고정이 가능해졌다.
   동작·작업폴더·아이콘은 그대로다.
2) 원인 2 — 고정 verb 를 한글/영문 문자열 하드코딩으로 찾았다. 이제 shell32.dll
   문자열 리소스 5386 에서 현지화된 이름을 읽어 1순위로 대조한다 (기존 패턴은 폴백).
3) 자동 고정이 막히면(Win10 1903+ · Win11) 아이콘을 선택한 탐색기 창을 띄워
   우클릭 → [작업 표시줄에 고정] 을 바로 누를 수 있게 했다. 안내문도 정확히 바꿨다.
4) 망고보드_작업표시줄고정.bat 신설 — 아이콘 재생성 없이 고정만
   (board/desktop_icon.py --pin-only).

버전 2.8.0 (Python 보드)
업데이트: P3_설정수정_카테고리매핑초기화 신설.
1) P3_필터단위_수집조건수정 을 복제하고, 목록·팝업 조작은 P5_101 의 검증된 로직
   (map_categories.py) 을 재사용 — 같은 화면(admin_group.php 목록 ·
   market_mapping_new 팝업 · admin_category_set.php)을 다루기 때문.
2) 입력: 사이트명 · 작업 URL(작업 시작 시 [선택조건으로 검색하기] 자동 클릭) ·
   작업행 범위 [부터]-[까지].
3) 행마다: [설정수정](market_mapping_new) → 팝업 → [검색필터 설정삭제]
   (config_remove('','Y')) → 팝업 닫기 → 다음 행.
4) --list-rows 로 초기화 없이 행 번호·ftid·필터명 확인 가능 (P5_101 과 동일).
5) 보드 P3_설정수정_카테고리매핑초기화 탭 — [행 목록 확인] · [초기화 시작]
   (되돌릴 수 없음 확인 대화상자) · [작업중단].

버전 2.7.3 (Python 보드)
업데이트: 망고보드_독립저장소올리기.bat 구문 오류 수정.
1) 원인 — scripts/*.ps1 이 BOM 없는 UTF-8 이라, Windows PowerShell 5.1 이
   한글 Windows 기본 코드페이지(CP949)로 읽었다. 한글·기호(→·※)가 2바이트로
   잘못 소비되면서 뒤따르는 큰따옴표를 삼켜 문자열이 깨지고,
   'Unexpected token }' 구문 오류가 났다 (publish-standalone.ps1:85).
2) scripts/*.ps1 4개 전부에 UTF-8 BOM 을 넣어 PowerShell 이 UTF-8 로 읽게 했다.
   install-all.ps1 도 같은 결함이 있었다 (따옴표 2개 소실).
3) board/tests/test_ps1_encoding.py 신설 — BOM 누락과 CP949 오해석 시
   따옴표 개수가 달라지는 경우를 잡는다.

버전 2.7.2 (Python 보드)
업데이트: P5_101 화면 검색결과 선택에도 성별 규칙 적용.
1) 원인 — 성별 배제(2.6.0)가 엑셀 카테고리를 고르는 단계에만 걸려 있었다.
   검색어는 카테고리의 리프뿐이라(예 '로퍼') 망고가 남성·여성을 함께 돌려주는데,
   pick_option 이 리프만 같으면 목록에 먼저 나온 항목을 골라 여성 필터가
   '남성신발 > 로퍼' 로 매핑됐다.
2) pick_option 에 필터명을 넘겨 반대 성별 항목을 걷어내고 같은 성별을 우선한다.
   반대 성별만 남으면 매핑하지 않고 사유를 로그에 남긴다.
3) 선택 직후 violates_gender 재검사 추가 — 위반이면 저장하지 않는다.
4) 성별 판정 오류 수정 — 'women' 안에 'men' 이 들어 있어 'Womens Shoes' 를
   남성으로 읽었다. 남성 판정 전에 여성 영문 표기를 제외하고,
   여성 표기에 woman·ladies 를 추가했다.

버전 2.7.1 (Python 보드)
업데이트: '몇 번째 행인지' 확인 기능 추가.
1) map_categories.py --list-rows — 매핑 없이 목록을 열어 1행부터 번호·ftid·
   필터명을 로그로 출력. 지정한 --row-from/--row-to 구간에 ★ 표시.
2) 보드 P5_101 탭에 [행 목록 확인] 버튼 추가 — 매핑 시작 전에 행 번호를 검증.
3) 번호는 [설정수정](market_mapping_new) 링크가 화면에 나오는 순서(위→아래)와
   같다.

버전 2.7.0 (Python 보드)
업데이트: 품목 계열 배타 · 하위 우선 검색.
1) 계열 사전(의류·신발·모자·선글라스·시계·가방·액세서리) 신설 — 다른 계열 카테고리는
   후보에서 제외한다 (의류↔신발, 선글라스↔시계 등). 계열 불명·잡화 계열은 남긴다.
2) 같은 계열이 없으면 잡화 계열에서 선택 (규칙3 폴백).
3) 전체 재검색 순서를 하위(1차) → 중위(2차) 로 교체, 하위 검색에 동의어 확장 적용.

버전 2.6.0 (Python 보드)
업데이트: 반대 성별 카테고리 절대 배제.
1) 여성 필터 → '남성' 용어가 든 카테고리(남성패션·남성신발 등) 전부 후보 제거,
   남성 필터 → '여성' 용어 카테고리 전부 제거. 최근접 지정 단계에도 동일 적용.
2) 최종 선택 직전 재검사 — 위반 시 배제 후 재선정 (로그 표기).
3) 반대 성별만 있어 고를 것이 없으면 매핑하지 않고 사유를 남긴다
   ('성별(여성) 조건에 맞는 카테고리 없음').

버전 2.5.2 (Python 보드)
업데이트: 보드 P5_101 입력 배치 정리.
1) 「작업 행 범위 [부터]-[까지]」 를 상품수집사이트 줄에서 떼어 「목록 URL」 아래로 이동.
2) 그 옆에 기준 안내 표기 — 목록 URL 검색결과의 행 번호(1부터, 양끝 포함).

버전 2.5.1 (Python 보드)
업데이트: '작업 대상 행이 없습니다' 수정.
1) 원인 — 행 스캔을 table#search_category 로 한정해, 설정수정 링크가 15개 있는
   화면에서도 0행으로 읽혔다(진단 로그로 확인).
2) 이제 표 구조에 기대지 않고 a[onclick*=market_mapping_new] 에서 closest('tr') 로
   행을 찾는다. 필터이름은 행의 텍스트 input → attr-uid 매칭 input → 행 첫 줄 순.
3) 모든 프레임 결과를 합치고 ftid 중복은 제거 (프레임에 중복 노출되는 화면 대응).

버전 2.5.0 (Python 보드)
업데이트: 카테고리 선택 규칙 4가지를 강제 적용.
1) 엑셀 범위 내 선택 (기존) — 확정 후 검사·교정.
2) 성별 고정 — 남성/여성이 구분된 필터는 같은 성별 경로에서만 선택,
   같은 성별이 없으면 무성별 경로 (다른 성별 배제).
3) 품목명 일치 — 의류·잡화·모자·선글라스·액세서리·신발이 필터명에 있으면
   그 명칭이 든 카테고리에서만 고르고, 상위→중위→하위 순으로 우선.
4) 의류 우선순위 — 패션의류잡화·패션의류·남성/여성(패션)의류를 먼저 선택
   (예: 남성-팬츠는 스포츠/레저보다 패션의류).
적용된 규칙은 로그에 [성별=남성 · 품목=의류] 로 표기.

버전 2.4.1 (Python 보드)
업데이트: 최적 카테고리는 반드시 엑셀 범위 내에서 선택.
1) matching.is_from / ensure_from 추가 — 결과가 엑셀 목록 밖이면 목록 내
   최근접 값으로 교정하고 로그에 남긴다.
2) 화면 검색 결과 선택도 엑셀 카테고리 기준으로 맞춘다 (필터명 기준 아님).

버전 2.4.0 (Python 보드)
업데이트: 매핑 누락 방지 — 정밀 분석 보강 · 저장 후 재검증 3회.
1) 동의어 사전 추가 (바라클라바↔방한모, 버킷햇↔벙거지, 비니↔니트모자,
   스니커즈↔운동화, 선글라스↔아이웨어 …) — 최근접 판단에 반영.
   리프가 동의어와 일치하면 높은 가중치.
2) 저장 후 재검증 — openmarket_cm_category_<코드>/_name_<코드> hidden 값으로
   실제 매핑 여부를 확인하고, 미매핑 마켓만 다시 매핑 후 저장. 최대 3회 반복.
3) 3회 후에도 남으면 경고 로그 + 결과에 unmapped 기록.

버전 2.3.0 (Python 보드)
업데이트: 최적 카테고리를 못 찾아도 반드시 하나 지정 · 3회 재시도.
1) 규칙 탐색(1·2-1~2-4) 실패 시 '3) 최근접 지정' 단계 추가 — 소재·용도/활용·성별·
   글자 유사도를 종합해 가장 가까운 카테고리 하나를 반드시 고른다(성별 불일치 감점,
   품목 포괄 카테고리 가점).
2) 매핑 실패(검색결과 없음·목록 선택 실패) 시 다른 카테고리로 최대 3회 추가 시도
   (매번 직전 후보 제외), 이후 행 끝에서 검색필터 설정저장으로 마무리.
3) find_category(force=False) 로 기존처럼 미검출 반환도 가능.

버전 2.2.0 (Python 보드)
업데이트: P5_101 구분 라디오 · 상품고시정보 팝업 대응.
1) 보드 입력에 라디오 추가 — 11번가(해외/국내/둘다) · 롯데ON(해외직구/일반/둘다),
   기본 '둘다'. 둘다면 한 행에서 두 구분을 각각 매핑한다.
   구분 라디오는 openmarket_seller_type2_<코드> 를 라벨로 찾아 강제 클릭.
2) 매핑 후 상품고시정보 팝업(#mapping_notify_<코드>)이 뜨면 닫고,
   직전 카테고리를 제외한 다음 최적 카테고리로 재매핑 후 설정저장 (최대 3회).
3) matching.find_category 에 exclude 지원 추가.

버전 2.1.1 (Python 보드)
업데이트: P5_101 체크 여부와 무관하게 행 범위로 작업.
1) '체크된 행이 없습니다' 로 멈추던 문제 — 이제 체크 상태를 보지 않고
   [부터]~[까지] 범위의 행을 그대로 처리한다 (로그에 체크 건수는 참고 표기).
2) 목록이 프레임 안에 있어도 찾도록 프레임 탐색 추가.
3) 행을 못 찾으면 진단 로그 출력 — 프레임별 url·tr 수·체크박스 수·설정수정 링크 수·
   onclick 예시.

버전 2.1.0 (Python 보드)
업데이트: P5_101 적용 범위 제한 (요건).
1) 수집사이트 musinsa.com 한정 — 다른 사이트는 실행 거부(보드 경고), 검증 후 개방.
2) 작업 대상은 [선택조건으로 검색하기] 결과 목록의 체크된 행만.
3) 작업 행 범위 입력 [부터]-[까지] 신설 (기본 1~5, 1부터·양끝 포함).
   범위가 뒤집히거나 잘못되면 자동 보정. 로그에 '체크 N건 중 a~b행 → m건 수행' 표기.

버전 2.0.1 (Python 보드)
업데이트: P5_101 최적 카테고리 탐색을 요건 명세대로 재구현 (matching.py 신설).
1) 필터명 해석 — 앞 2조각(브랜드-사이트) 무시, 상위·중위·하위로 분해.
   하위는 붙인말·토큰·반쪽으로 전개 (버킷/사파리 햇 → 버킷햇·버킷·햇·사파리,
   바라클라바 → 바라클라바·바라·클라바).
2) 찾는 순서 — 1)단계일치 → 2-1)상위→중위→하위 → 2-2)중위 전체 →
   2-3)하위 전체 → 2-4)품목별 포괄(의류/신발/잡화).
3) 상위 보조 후보(패션잡화·의류잡화·패션의류잡화)와 중위 동의어(소품↔잡화) 반영.
4) 어느 단계에서 찾았는지 실행로그·드라이런에 표기.

버전 2.0.0 (Python 보드)
업데이트: P5_101_카테고리매핑_필터세부설정 신설.
0) 마켓별 입력필드 #openmarket_category_search_text_<코드>, 결과 리스트박스는
   list_/list2_ 두 벌 중 보이는 select 우선 (11번가·롯데ON).
1) 초기 1회 — 상품수집사이트 선택(select[name=site_id]) → [선택조건으로 검색하기]
   (search_filter('search')) → 마켓별 카테고리 엑셀 로드(P5 출력 양식, 폴더 자동인식).
2) 1단계 루프 — 체크된 행마다 필터이름 읽기 → [설정수정](market_mapping_new(ftid))
   → 팝업(admin_category_set.php?ps_ftid=) → [AI 자동 매핑 시작하기]
   (search_recommend_category_all) → 마켓별 매핑 → [검색필터 설정저장](config_save)
   → 모달 닫기 → 다음 행.
3) 2단계 루프 — 옥션2.0·11번가·G마켓2.0·스마트스토어·쿠팡·롯데ON 순서로
   필터이름↔엑셀 카테고리 유사도 비교 → 검색필드 입력
   (#openmarket_category_search_text_<코드>) → [검색] → 결과 목록 선택.
4) 점수 0.34 미만이면 매칭하지 않고 건너뛴다 (오매핑 방지). --dry-run 으로
   브라우저 없이 매칭 결과만 확인 가능.
5) 보드 P5_101 탭 — 사이트·엑셀 폴더(인식 결과 표시)·목록 URL·시작/중단·로그.

버전 1.9.3 (Python 보드)
업데이트: 구분 라디오를 항상 클릭해 체크.
1) 이미 체크된 라디오는 check() 가 no-op 이라 onclick=change_category_list 가
   실행되지 않았다. 이제 force 클릭으로 항상 실행시키고, 기본 선택이었으면
   로그에 '(기본 선택 → 재클릭)' 을 남긴다.

버전 1.9.2 (Python 보드)
업데이트: 롯데ON 일반카테고리 누락 수정.
1) 원인 — 두 select(list_/list2_)이 같은 name 에 display 로만 전환되는데,
   '옵션이 많은 쪽'을 골라 숨은 select 의 직전 구분 목록을 읽을 수 있었다.
   이제 보이는 select(getComputedStyle·offsetParent) 을 우선 읽는다.
2) 구분 전환 후 목록 지문이 직전 구분과 달라질 때까지 대기(최대 15초),
   끝까지 같으면 경고 로그. 읽은 select id·건수를 로그에 표기.

버전 1.9.1 (Python 보드)
업데이트: P5 가 카테고리설정 화면을 전용 탭에서 열도록 수정.
1) 원인 — CDP 로 붙은 현재 탭(수집조건수정 팝업일 수 있음)에서 그대로 이동해
   엉뚱한 창이 바뀌었다. 이제 admin_category_set.php 탭이 있으면 재사용,
   없으면 새 탭을 만들어 앞으로 가져온다.
2) --ftid 옵션 추가 — 검색필터별 ps_ftid 로 접근 URL 생성 (기본 790).

버전 1.9.0 (Python 보드)
업데이트: P5 카테고리 구분(해외/국내) 각각 추출.
1) 11번가 — 해외카테고리 / 국내카테고리, 롯데ON — 해외직구 카테고리 / 일반카테고리.
   구분 라디오(openmarket_seller_type2_<코드>)를 label 내 span 텍스트로 찾아 선택하고
   change_category_list 반영을 기다린 뒤 [전체카테고리] 조회.
2) 엑셀에 '구분' 열 추가 — 마켓·구분·1~6단계·전체경로. 두 구분을 각각 담는다.
3) ALL 실행 시 마켓 × 구분 조합을 순서대로 추출하고 조합별 행수를 로그에 남긴다.

버전 1.8.2 (Python 보드)
업데이트: P5 구현 제외 마켓 고정.
1) LFMall · 머스트잇 · 쇼피 · 큐텐(일본) · 플레이오토(EMP) 는 추출 대상 제외.
   EXCLUDED_MARKETS 로 명시하고 ALL 실행에서도 걸러낸다. 직접 지정하면
   '구현 제외 마켓입니다' 로 종료.

버전 1.8.1 (Python 보드)
업데이트: P5 전체 마켓 지원.
1) 마켓 6종 — 옥션2.0(AUC20) · 11번가(11ST) · G마켓2.0(GMK20) ·
   스마트스토어(SMART) · 쿠팡(COUP) · 롯데ON(LTON).
2) --market ALL — 한 번에 6개 마켓을 순서대로 추출해 한 엑셀에 마켓 열로 정리.
3) 목록 select 가 list_/list2_ 두 벌인 마켓(11번가·롯데ON) 대응 — 둘 다 읽어
   항목이 많은 쪽 사용.
4) 보드 P5 마켓 리스트박스에 '전체 마켓 일괄' 추가.

버전 1.8.0 (Python 보드)
업데이트: P5_카테고리_엑셀추출 신설.
1) admin_category_set.php 에서 마켓(기본 옥션2.0/AUC20) [전체카테고리] 클릭 →
   select#openmarket_category_search_list_<마켓> 옵션 전체를 읽어
   카테고리분류표(1~6단계) 엑셀로 저장. 안내문구·중복 제외, 6단계 초과는 6단계에 합침.
2) 보드 P5 탭 — 마켓 리스트박스·접근URL·저장경로·[추출 시작]·[엑셀 열기]·로그.
3) --from-text 로 브라우저 없이 목록 텍스트만으로도 엑셀 생성 가능.

버전 1.7.6 (Python 보드)
업데이트: 수집사이트 드롭다운 미검출 실제 원인 수정.
1) 원인 — 다른 탭(select 0개)을 보고 있었다. pick_list_page 로 site_id 가 있는
   탭/프레임을 골라잡는다 (URL getGoodsCategory 폴백).
2) 시작 1회 단계이므로 수집사이트 대기는 5초(T_SITE), 행 반복 단계는 빠른 값 유지.
3) 진단 로그 강화 — 탭·프레임별 url · select 이름 · 버튼 라벨을 그대로 출력.

버전 1.7.5 (Python 보드)
업데이트: 수집사이트 드롭다운 미검출 수정.
1) 대기 0.3초 → 1초 (T_POPUP). 0.3초는 목록이 덜 뜬 상태에서 조회해 실패했다.
2) apply_site_filter/read_site_options 가 드롭다운을 '기다린 뒤' 찾는다
   (1초 단위 최대 3회, 뜨면 즉시 진행).
3) 프레임 전체 순회 + 「수집사이트」 옵션 텍스트 폴백 + 미검출 시 화면 select
   목록을 로그에 출력(원인 파악용).

버전 1.7.4 (Python 보드)
업데이트: 팝업 대기 예산 0.3초.
1) T_POPUP = 300ms — 팝업 열기(expect_popup)·직접 오픈(goto)·번역옵션 드롭다운 대기.
2) 드롭다운은 0.3초 단위로 최대 4회 확인(POPUP_TRIES) — 뜨는 즉시 진행하므로
   빠른 경우 손해가 없고, 서버가 느릴 때만 최대 1.2초까지 버틴다.

버전 1.7.3 (Python 보드)
업데이트: P3_필터단위_수집조건수정 — 순서 유지 · 속도 최대화 · 닫기 버튼.
1) 저장하기 직후 바로 옆 [닫기](onclick=window.close()) 를 눌러 팝업을 즉시 종료하고
   다음 행으로 진행. 못 찾으면 window.close() 직접 호출 → 그래도 남으면 강제 종료.
2) 단계별 대기 10배 축소 (클릭 1.5s · 필드 2s · 읽기 0.2s · 닫힘 0.8s ·
   행간 0.02s · 검색후 0.15s). 상수 T_*/GAP_* 한 곳에서 관리.
3) 목록 접속은 URL 직접 이동(빠른 경로) 우선, 실패 시에만 기존 단계 절차.
4) 순서(수집조건수정 → 팝업 → 번역조건 반영 → 저장 → 닫기 → 다음행)는 그대로.

버전 1.7.2 (Python 보드)
업데이트: 부트스트랩 복사 누락 방지.
1) AI보드 루트 망고보드_시작.bat 의 robocopy 에 /IS /IT 추가 — 타임스탬프가 같은
   옛 파일(app.py 등)이 남아 버전만 바뀌는 문제를 막는다.
2) board/tests/test_bootstrap_bat.py 신설 — 단계·강제덮어쓰기·데이터보호·대상폴더 검증.

버전 1.7.1 (Python 보드)
업데이트: 독립 저장소 올리기 원클릭.
1) 망고보드_독립저장소올리기.bat 신설 — scripts/publish-standalone.ps1 호출.
2) publish 스크립트 재작성: 폴더 상태를 자동 판별한다 —
   (A) 독립 저장소 클론이면 그대로 push, (B) 단독 폴더면 그 자리에서 git init 후
   push(이후 git pull 가능), (C) AI보드 하위 폴더면 임시 복사본으로 push(원본 불변).
   push 성공 시 이후 아이콘 자동갱신이 독립 저장소를 쓰게 된다.

버전 1.7.0 (Python 보드)
업데이트: 바탕화면 아이콘 실행 시 최신 소스 자동 반영.
1) board/auto_update.py 신설 — VERSION.txt 를 원격과 비교해 더 높을 때만 갱신.
   .git 이 있으면(자기 폴더 또는 부모 저장소) git pull, 없으면 GitHub ZIP 에서
   필요한 파일만 덮어쓴다. 독립 repo 우선 → 부모 repo(main) 폴백.
2) run.bat 이 보드 시작 전에 자동갱신을 수행 (--noupdate 로 생략).
3) 사용자 데이터 보호: run-logs · output · .chrome-profile · *.xlsx ·
   .translate_options.json · .site_options.json · *.lnk 는 덮지 않음.

버전 1.6.2 (Python 보드)
업데이트: 바탕화면 아이콘을 작업표시줄에 고정 가능하도록.
1) board/desktop_icon.py — 핀 폴더(User Pinned\TaskBar) 복사 + 셸 「작업 표시줄에
   고정」 verb 호출 + 시작메뉴 등록. 고정 성공/차단 여부를 메시지로 알림.
2) --no-pin 으로 고정 없이 아이콘만 생성 가능.

버전 1.6.1 (Python 보드)
업데이트: 수집사이트 선택 후 [선택조건으로 검색하기] 버튼을 명시적으로 클릭.
(라벨 정확 일치를 1순위 선택자로, 기존 '검색' 폴백은 유지)

이전(1.6.0): P3_필터단위_수집조건수정 — 수집사이트 리스트박스 · 팝업 저장 흐름 · 첫화면 URL.
1) 입력 리스트박스 2개: 수집사이트(select[name=site_id] — 4910.kr /
   ABCmart.a-rt.com / HIVER.co.kr / MUSINSA.com / Zara.com/de) + 번역옵션.
   수집사이트를 고르면 목록 화면에 검색을 걸고 그 결과 행만 처리한다.
2) 실제 흐름대로 재구현: 행의 [수집조건수정](modify_filter(fuid)) → 팝업창
   (/mall/admin/admin_group_modify.php?ps_mode=modify_filter&ps_fuid=…) →
   번역 후 저장 선택 → 하단 [저장하기](set_save()) → 알림 자동확인 →
   팝업(window.close()) 닫힘. 목록 페이지를 떠나지 않아 검색조건이 풀리지 않는다.
3) 첫 화면 URL 기본값: https://tmg1898.cafe24.com/mall/admin/shop/getGoodsCategory.php
4) 폴백: 팝업 클릭 실패 시 팝업 URL 직접 오픈, 저장 버튼 미검출 시 set_save() 호출.

이전(1.5.2): 요건에 없던 보드 [바탕화면 아이콘 만들기] 버튼 제거.
이전(1.5.1): 바탕화면 아이콘은 실행파일 1개만 (실제 활성 바탕화면).
이전(1.5.0): P3_필터단위_수집조건수정 신설 — 번역옵션 리스트박스.
이전(1.4.3): 망고보드를 부모 저장소 main 으로 가져옴. AI보드와 별개의 독립 보드.
