* 최종 버전 --> 1.4.3

* 로컬PC 보관   --> Not-OK
* Vercel 배포  --> Not-OK
* GitHUB Commit --> OK

## 1. 요구사항 요청 받은 날짜·시간

- 1차: 2026-08-22 01:18 KST
- 2차(보충): 2026-08-22 01:23 KST

## 2. 요구사항 반영 완료 날짜·시간

2026-08-22 01:30 KST

## 3. 반영된 프로그램 버전

1.4.3 (망고보드) — 부모 저장소 AI보드는 2.1.56

## 4. 최종본 위치

- GitHub: https://github.com/waterstar21g-png/AI_Program_Main_Board
  (브랜치 `cursor/bring-in-mango-board-4ef3` → `main` PR, 폴더 `Mango_Helper_AI_Board/`)
- 로컬PC: 미반영

## 5. 사용자 작성 원문 전체 (총 2건)

[원문 1]
```
망고 보드를 가져와
```

[원문 2]
```
망고보드 는  AI보드 처럼 별도의 보드 기능임을 명심해...

AI보드와는 별개의 독립적인 기능리아구...
```

---

## 반영 내용

### 1. 개발 브랜치 → main

망고보드가 부모 저장소 `main` 으로 들어왔으므로, 설치·업데이트 경로가 더 이상
`cursor/mango-helper-ai-board-0c73` 을 가리키지 않게 정리했다.

| 파일 | 변경 |
|------|------|
| `망고보드_한번에설치.bat` | 부모 폴백 clone `-b main` |
| `scripts/install-all.ps1` | `$ParentBranch = "main"` |
| `scripts/pull-update.ps1` | 부모 저장소 fetch/pull `main` |
| `programs/registry.json` | `parent_branch: "main"` |
| `README.md` · `REPOSITORY.md` · `PC_SETUP.md` · `PC_준비완료.md` · `PC_클론가이드.md` · `GITHUB_SETUP.md` | 브랜치 안내 정정 |

독립 repo 우선 → 없으면 부모 `main` 폴백 순서는 그대로 유지.

### 2. AI보드와 별개의 독립 보드 (원문 2)

- `README.md` 최상단에 **「★ AI보드와 별개의 독립 보드」** 절 신설 — 실행·버전·
  요건문서·업데이트·PC폴더가 모두 분리됨을 표로 명시
- `REPOSITORY.md` 의 "AI board 와의 관계" 를 "별개의 독립 보드" 로 재작성
- AI보드 쪽 문서(`README.md` · `PROJECTS.md`)에서도 망고보드를 **AI보드 프로그램
  목록(P1~P3)에 넣지 않고** 별도 보드로만 표기

### 3. 독립성 회귀 테스트

`board/tests/test_board_independence.py` (7건)

- 루트 자기완결성 (`VERSION.txt` · `run.bat` · `board/app.py` · `registry.json`)
- `self_update` 가 자기 repo(`Mango_Helper_AI_Board`) `main` 대상 · 루트를 자기 폴더로 계산
- 버전은 자기 `VERSION.txt` 에서만 읽음
- `terms` 로 AI보드와 이름 구분
- 살아있는 스크립트·문서에 개발 브랜치 참조 없음
- 파이썬 코드가 AI보드를 참조하지 않음

`docs/일별_사용자요건/` 의 과거 SR 문서는 당시 기록이라 수정하지 않았다.
