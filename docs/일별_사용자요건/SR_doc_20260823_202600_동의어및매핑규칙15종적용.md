* 최종 버전 --> v2.44.0 (Python 보드, P5_101_카테고리매핑_필터세부설정)
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK (해당 없음: 데스크톱/카테고리매핑 프로그램, Vercel 배포 대상 아님)
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-23 11:26~11:36 (UTC) / 20:26~20:36 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-23 20:42 (KST)
3. 반영된 프로그램 버전: v2.44.0 (VERSION.txt)
4. 최종본 위치: 로컬PC(`/workspace`) — GitHub(현재 작업 브랜치, 커밋·푸시 완료). Vercel 해당 없음.
5. 사용자 작성 원문 전체 (편집·요약 없음):

```
1. 자켓 = 재킷  상호동의어로 간주하고, 
   어느 한쪽 단어로 검색이 안될시 -->  다른 단어로 검색을 시도 할것

2. 임부복 -이 들어간 카테고리는 매핑에서 제외할것 

3. "스포츠"가 들어간 단어는 카테고리 매핑 제외 대상 단어에 포함하지 말것.

4. 우선순위 적용 : 여성의류 > 패션의류 > 패션의류잡화  

5. 캐쥬얼, 기타 등 단어는 가장 일반화된 범주로 간주할것 
  (일치하는 카테고리 단어가 없을때 해당 단어로 매핑 할것)     

6. 패딩, 점퍼, 패딩점퍼, 야상, 패딩다운, 사파리, 바람막이 등은 유사등의어 등록후,
   정확한 단어가 없는 경우 --> 카테고리 매핑에 추가적으로 참고 할것  

7. 가디건, 스웨터, 외출복 은 유사동의어로 간주할것

8. 정장 = 수트 : 두단어는 동일어로 간주하여 검색에 활용할것

9. 정장구두=로퍼  는 동의로 간주할것.
   캐쥬얼구두=옥스퍼드 는 동의어로 간주할것

10. 뮬=블로퍼=캐쥬얼구두 는 상호등의로 간주할것

11. 슬리퍼 = 슬립온 = 플립플랍 = 수영장 샌들 = 비치 신발 은 동의어로 간주할것

12. 단화=하이힐(미들)=하이힐(로우)=뮬=블로퍼 등은 동의어로 간주할것

13. 모자=야구모자=스포트모자 는 동의어로 간주할것

14. 비니=머리수건=햇=햇볕가리개 는 유사동의로 간주할것

15. 사파리=햇빛가리개=햇=낚시모자 는 유사동의로 간주할것
```

--- 이하 구현 내용 ---

## 1. 품목 동의어 및 유사동의어 사전 체계화 (`matching.py`)
`ITEM_SYNONYMS` 및 `expand_synonyms()` 에 사용자 지정 15종 동의어 관계를 체계적으로 연결:
* **의류 계열**:
  - `자켓` ↔ `재킷` (상호 동의어, 한쪽 검색 실패 시 다른 쪽 자동 검색)
  - `패딩` ↔ `점퍼` ↔ `패딩점퍼` ↔ `야상` ↔ `패딩다운` ↔ `사파리` ↔ `바람막이` (유사동의어)
  - `가디건` ↔ `스웨터` ↔ `외출복` ↔ `니트` (유사동의어)
  - `정장` ↔ `수트` (동일어)
* **신발 계열**:
  - `정장구두` ↔ `로퍼` (동의어)
  - `캐쥬얼구두` ↔ `옥스퍼드` (동의어)
  - `뮬` ↔ `블로퍼` ↔ `캐쥬얼구두` (상호동의어)
  - `단화` ↔ `하이힐(미들)` ↔ `하이힐(로우)` ↔ `미들힐` ↔ `로우힐` ↔ `뮬` ↔ `블로퍼` (동의어)
  - `슬리퍼` ↔ `슬립온` ↔ `플립플랍` ↔ `수영장 샌들` ↔ `비치 신발` (동의어)
* **모자/잡화 계열**:
  - `모자` ↔ `야구모자` ↔ `스포트모자` ↔ `스포츠모자` (동의어)
  - `비니` ↔ `머리수건` ↔ `햇` ↔ `햇볕가리개` (유사동의어)
  - `사파리` ↔ `햇빛가리개` ↔ `햇` ↔ `낚시모자` (유사동의어)

## 2. 절대 배제 단어 및 허용 단어 조정
* `FORBIDDEN_BASE_WORDS` 에서 **"스포츠"**를 제외하여 스포츠 관련 의류/잡화가 정상 매핑되도록 허용.
* `FORBIDDEN_BASE_WORDS` 에 **"임부복", "임부", "임산부", "마터니티"**를 추가하여 임부 카테고리를 완전히 배제.

## 3. 우선순위 및 일반화 범주 설정
* 여성의류 우선순위: `여성의류 > 패션의류 > 패션의류잡화` 점수 체계 유지 및 검증.
* `SAFE_FALLBACK_WORDS` 에 `캐쥬얼, 캐주얼, 캐쥬얼의류, 기타잡화, 기타소품, 기타` 등록하여 일치하는 카테고리가 없을 때 가장 일반화된 범주로 매핑하도록 지원.

## 4. 검증 결과
* `tests/test_domestic_foreign_and_forbidden.py` 에 15종 요건 전용 단위 테스트 추가 및 검증.
* 전체 483개 테스트 100% 정상 통과.
