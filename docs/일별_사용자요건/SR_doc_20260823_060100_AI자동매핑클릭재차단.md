* 최종 버전 --> 2.32.1

* 로컬PC 보관   --> Not-OK
* Vercel 배포  --> Not-OK
* GitHUB Commit --> OK

## 1. 요구사항 요청 받은 날짜·시간

2026-08-23 06:01 KST

## 2. 요구사항 반영 완료 날짜·시간

2026-08-23 06:05 KST

## 3. 반영된 프로그램 버전

2.32.1 (망고보드 · P5_101_카테고리매핑_필터세부설정)

## 4. 최종본 위치

- GitHub: 브랜치 `cursor/p5-101-category-matching-redefine-c027`
- 로컬PC: 미반영
- Vercel: 해당 없음 (로컬 실행 Python 보드)

## 5. 사용자 작성 원문 전체 (편집·요약 없음)

```
아...새키 말 진짜 안듣네....

너....AT자동매핑 하하고 햬어...하지말라고 했어>?
```

---

## 반영 내용

### 원인

요건재정의(2026-08-22, SR: `카테고리매칭요건전면재정의.md`) 원문 C-3
"팝업창에서 → AI자동매칭 시작하기 버튼클릭 \*\*\*\*당분간 로직 수행
않함\*\*\*\*"을, "버튼은 클릭하되 그 결과에 우리 로직을 얹지 않는다"로
잘못 해석해 `map_one_row()` 에서 v2.27.0 때 꺼 두었던
`click_ai_mapping(popup, progress=progress)` 호출을 v2.29.0에서 다시
켰다. 사용자가 이를 지적 — 실제 의도는 "이 단계(버튼 클릭) 자체를
당분간 수행하지 않는다"였다.

### 수정

`map_categories.py` `map_one_row()` 에서 `click_ai_mapping()` 호출을
다시 주석 처리했다(v2.27.0과 동일한 상태로 원복). v2.27.0에서 처음
끈 이유 — "이 버튼이 필드를 먼저 채운 뒤 우리 검색이 실패하면 검증
안 된 값이 그대로 저장된다" — 가 여전히 유효하므로, 이 단계 자체를
건너뛴다. 마켓별 매핑은 이 버튼과 무관하게 항상 엑셀에서 확정한 값으로
검색·선택하며, 실패하면 `clear_market_category` 로 필드를 명시적으로
비우는 안전장치는 그대로 유지된다.

## 검증

| 대상 | 결과 |
|------|------|
| `P5_101_카테고리매핑_필터세부설정/tests` | **234 passed** (기존과 동일 — 이 함수 호출 여부를 직접 검증하는 테스트는 없었음) |
