* 최종 버전 --> 1.8.2

* 로컬PC 보관   --> Not-OK
* Vercel 배포  --> Not-OK
* GitHUB Commit --> OK

## 1. 요구사항 요청 받은 날짜·시간

2026-08-22 11:54 KST

## 2. 요구사항 반영 완료 날짜·시간

2026-08-22 11:56 KST

## 3. 반영된 프로그램 버전

1.8.2 (망고보드)

## 4. 최종본 위치

- GitHub: 브랜치 `cursor/mango-p3-speed-4ef3`
- 로컬PC: 미반영

## 5. 사용자 작성 원문 전체 (스크린샷 1장 첨부)

```
LFMall 카테고리, 머스트잇 카테고리, 쇼피 카테고리,큐텐(일본) 카테고리,플레이오토(EMP) 카테고리

이것들은 구햔대상에서 모두 제외한다....
```

---

## 반영 내용

해당 5개는 애초에 대상 목록(`MARKETS`)에 넣지 않았으므로 동작 변화는 없다.
다만 나중에 실수로 포함되지 않도록 **제외 목록을 코드에 명시**하고 테스트로 고정했다.

```python
EXCLUDED_MARKETS = {
    "LFMALL": "LFMall",
    "MUSTIT": "머스트잇",
    "SHOPEE": "쇼피",
    "QOO10JP": "큐텐(일본)",
    "PLAYAUTO": "플레이오토(EMP)",
}
```

- `markets_to_run()` 이 `ALL` 전개 결과에서도 제외 코드를 걸러낸다
- 제외 마켓을 직접 지정하면 `구현 제외 마켓입니다: <표기>` 로 즉시 종료
- README 에 「구현 제외 (요건)」 절 추가

추출 대상은 6개로 확정 — 옥션2.0 · 11번가 · G마켓2.0 · 스마트스토어 · 쿠팡 · 롯데ON.

## 검증

`P5_카테고리_엑셀추출/tests` **26 passed** — 신규 3건: 제외 목록이 대상 목록과
겹치지 않음, `ALL` 이 6개만 실행, 제외 마켓 직접 지정 시 실행 목록이 빈다.
