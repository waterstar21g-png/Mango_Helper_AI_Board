* 최종 버전 --> 1.5.1

* 로컬PC 보관   --> Not-OK
* Vercel 배포  --> Not-OK
* GitHUB Commit --> OK

## 1. 요구사항 요청 받은 날짜·시간

2026-08-22 02:03 KST

## 2. 요구사항 반영 완료 날짜·시간

2026-08-22 02:06 KST

## 3. 반영된 프로그램 버전

1.5.1 (망고보드)

## 4. 최종본 위치

- GitHub: https://github.com/waterstar21g-png/AI_Program_Main_Board/pull/142
  (브랜치 `cursor/bring-in-mango-board-4ef3`)
- 로컬PC: 미반영

## 5. 사용자 작성 원문 전체

```
실행화일 에만 반영하라고.,..
```

---

## 반영 내용

이전 동작은 바탕화면 후보 폴더(기본·OneDrive·한글 「바탕 화면」)마다 아이콘을
만들고, 프로젝트 폴더에도 드래그용 사본을 하나 더 뒀다. 요건대로 **실행파일
아이콘 1개만** 만들도록 바꿨다.

| 항목 | 변경 |
|------|------|
| 위치 결정 | 레지스트리 `HKCU\…\Explorer\Shell Folders` 의 `Desktop` 값을 읽어 **실제 활성 바탕화면** 한 곳만 사용 (OneDrive·한글 폴더 리디렉션 자동 반영) |
| 생성 개수 | `망고보드.lnk` → `run.bat` **1개** |
| 이전 동작 | `--all` 옵션으로만 (후보 전부 + 프로젝트 폴더 사본) |
| 실패 처리 | 바탕화면을 못 찾으면 그 사유를 그대로 반환 |

레지스트리 조회는 `reg query` 출력을 파싱하며(경로에 공백이 있어도 안전),
읽기 실패 시 기존 환경변수 후보 중 첫 번째로 폴백한다.

## 검증

`board/tests` **23 passed** — 기본은 1개만 생성 / `--all` 은 사본 포함 /
레지스트리 값 우선 선택 / 공백 포함 경로 파싱 / 바탕화면 미검출 시 메시지.
