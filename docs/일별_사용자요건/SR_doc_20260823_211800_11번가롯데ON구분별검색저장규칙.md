* 최종 버전 --> v2.46.0 (Python 보드, P5_101_카테고리매핑_필터세부설정)
* 로컬PC 보관 --> OK
* Vercel 배포 --> Not-OK (해당 없음: 데스크톱/카테고리매핑 프로그램, Vercel 배포 대상 아님)
* GitHUB Commit --> OK

1. 요구사항 요청 받은 날짜·시간: 2026-08-23 12:18 (UTC) / 21:18 (KST)
2. 요구사항 반영 완료 날짜·시간: 2026-08-23 21:25 (KST)
3. 반영된 프로그램 버전: v2.46.0 (VERSION.txt)
4. 최종본 위치: 로컬PC(`/workspace`) — GitHub(현재 작업 브랜치, 커밋·푸시 완료). Vercel 해당 없음.
5. 사용자 작성 원문 전체 (편집·요약 없음):

```
11번가, 롯데on 은 아래 로직 적용 

A. *** 입력필드 구분이 : "국내"로 선택된 경우 

1. 해외 또는 해외직구 ::  라디오 선택후 
    => "의류" 검색후 임의의 리스트로 저장(단축키 : ALT+S) 

2. 국내 또는 일반 ::  라디오 선택후
    => "최종 카테고리명" 입력 검색후 저장(단축키 : ALT+S)

B. *** 입력필드 구분이 : "해외"로 선택된 경우

1. 해외 또는 해외직구 ::  라디오 선택후 
    => "해외명품" 검색후 임의의 리스트로 저장(단축키 : ALT+S) 

2. 국내 또는 일반 ::  라디오 선택후
    => "최종 카테고리명" 입력 검색후 저장(단축키 : ALT+S)
```

--- 이하 구현 내용 ---

## 1. 11번가 및 롯데ON 라디오 구분별 검색·저장 순서 로직 구현 (`map_categories.py`)
11번가(`11ST`) 및 롯데ON(`LTON`) 매핑 시 입력필드 `구분` 값에 따라 다음 절차를 순차적으로 수행:

* **A. 구분이 "국내"인 경우**:
  1. `해외카테고리`(11번가) / `해외직구 카테고리`(롯데ON) 라디오 클릭 선택
     ➔ 검색창에 `"의류"` 입력 후 검색
     ➔ 검색 결과 목록의 첫 번째 유효 항목 선택 후 저장 (Alt+S)
  2. `국내카테고리`(11번가) / `일반카테고리`(롯데ON) 라디오 클릭 선택
     ➔ 검색창에 엑셀에서 확정한 `"최종 카테고리명"` 입력 후 검색
     ➔ 일치 항목 선택 후 저장 (Alt+S)

* **B. 구분이 "해외"인 경우**:
  1. `해외카테고리`(11번가) / `해외직구 카테고리`(롯데ON) 라디오 클릭 선택
     ➔ 검색창에 `"해외명품"` 입력 후 검색
     ➔ 검색 결과 목록의 첫 번째 유효 항목 선택 후 저장 (Alt+S)
  2. `국내카테고리`(11번가) / `일반카테고리`(롯데ON) 라디오 클릭 선택
     ➔ 검색창에 엑셀에서 확정한 `"최종 카테고리명"` 입력 후 검색
     ➔ 일치 항목 선택 후 저장 (Alt+S)

## 2. 검증 결과
* 전체 488개 단위 테스트 100% 정상 통과 (484 pass, 4 skip).
